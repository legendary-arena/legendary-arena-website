# Legendary Arena Website — Vision

**Status:** v1 — decisions locked, brand identity v1 LOCKED (WP-002 + WP-003 done 2026-05-07; tonal iteration 2026-05-08 under §9.1 Early Lock Revision Window; cross-site consumption fully verified — WP-007a locked 2026-05-10 for `play.*`, WP-007b locked 2026-05-11 for `cards.*`)
**Repo:** github.com/legendary-arena/legendary-arena-website
**Production URL:** https://www.legendary-arena.com
**Last updated:** 2026-05-12

> **Authority:** This document is the top-level vision for the marketing
> site. It is subordinate to no other document in this repo. If a conflict
> arises with a Work Packet, README, or implementation detail, this
> document wins.

---

## Purpose

The Legendary Arena website is the **marketing landing for the Legendary
Arena game**. Its primary jobs:

1. Tell visitors what Legendary Arena is — clearly and quickly.
2. Funnel interested visitors to **play.legendary-arena.com** (the live game).
3. Provide a stable home for devblog, news, and announcements.

The site is **not** the game itself. It does not contain card data, deck
builders, or interactive gameplay. Those live at sister subdomains.

## Audience

- **Primary:** Players of the original Legendary: A Marvel Deck Building
  Game looking for a digital adaptation; deck-building game enthusiasts
  discovering LA for the first time.
- **Secondary:** Press, partners, and IP holders evaluating the project.
- **Tertiary:** Future contributors, if/when the project opens to
  outside help.

## Global invariants

These rules apply to all implementation and future changes. Violations
are bugs.

- The site remains **static-only**. No runtime APIs, no client-side data
  fetching from LA infrastructure.
- The site **does not depend on availability** of `play.*`, `registry.*`,
  or `api.*` to render or function.
- All cross-site navigation must **degrade gracefully** if a target is
  unavailable (a broken link is acceptable; a broken page is not).
- **Brand tokens** (`brand-tokens.css`) are the **single source of truth**
  for colors, typography, and spacing. No hardcoded values unless
  explicitly justified with a `// why:` comment.
- All builds must be **deterministic and reproducible locally**. The
  command that builds the site in CI must match the local command
  byte-for-byte.
- **Breaking changes to the brand token contract** require a major
  version bump AND coordinated updates to consuming sites BEFORE the new
  version publishes.
- No change may violate a locked decision without:
  - updating this document, AND
  - adding a Decisions log entry.

## Architecture — three sites, one product

Legendary Arena has three public-facing properties on three subdomains,
sharing a unified brand identity:

| Subdomain | Role | Tech |
|---|---|---|
| `www.legendary-arena.com` | Marketing landing (this repo) | Hugo, static |
| `play.legendary-arena.com` | Live game client | Vue (arena-client), static |
| `cards.legendary-arena.com` | Card registry browser | Vue (registry-viewer), static |

All three are deployed independently on Cloudflare Pages with the
following guarantees:

- Each site is **fully functional in isolation** — no runtime coupling.
- Cross-site navigation is **link-based only** — no shared runtime state
  or message passing.
- **Failure of one subdomain must not degrade the others** beyond broken
  links.

The registry migrated to `cards.legendary-arena.com` on 2026-07-16,
aligning it with the LA brand (Decisions log; supersedes the 2026-05-07
"stays at `cards.barefootbetters.com` for v1" entry). The migration
landed on `cards.legendary-arena.com` rather than the originally
projected `registry.legendary-arena.com`. The old domain 301-redirects
to the new one via a zone-level Redirect Rule on the
`barefootbetters.com` CF zone (deployed + verified 2026-07-16; path and
query string preserved — same mechanism as WP-006's apex→www rule).

A future `api.legendary-arena.com` (Render-hosted Node server) will serve
the game backend; this site does not interact with it directly.

### Cross-site contract

The three sites share exactly one runtime artifact: brand tokens.

- **Shared asset:** `https://www.legendary-arena.com/brand-tokens.css`
- **Versioned and backward-compatible** within a major version
- **Consumers:** `play.legendary-arena.com` and the registry viewer
  (`cards.legendary-arena.com`; the former `cards.barefootbetters.com`
  301-redirects to it) must not assume internal structure
  beyond the documented CSS custom properties (`--la-color-*`,
  `--la-font-*`, `--la-space-*`)
- **Local fallback:** consumers SHOULD bundle a local copy of the tokens
  as a safety net for transient unavailability of www
- **Breaking changes** trigger a coordinated version bump across all
  consuming sites per Global invariants

## In scope (v1)

- **Home page** — hero, value prop, primary CTA to play.*
- **About page** — what LA is, who's behind it, project status
- **Blog** — devblog, dev journals, release notes; low cadence acceptable
- **Search** (Pagefind) — across blog/marketing content only
- **Unified header + footer** — links to play.*, registry.*, social, legal
- **Brand token contract** — colors/type/spacing source of truth for all three sites
- **Performance budget** — sub-1s LCP on the home page
- **SEO baseline** — meta tags, Open Graph, Twitter Cards, Schema.org
  JSON-LD, sitemap.xml, robots.txt, Search Console submission
  (per WP-008)

## Out of scope (v1)

Explicitly excluded:

- **Card browsing** — that's `registry.*`'s job
- **Deck building** — that's `play.*`'s job
- **User accounts / authentication** — handled by the game, not this site
- **Comments on blog posts** — moderation overhead too high for v1
- **Forums, community hub** — Discord or similar fills this need; site
  links out
- **In-depth player documentation** — deferred until demand is proven

## Permanent non-goals

These are explicitly out of scope for **all future versions** of this
site, not just v1:

- This site never hosts the game client.
- This site never authenticates users.
- This site never depends on live card or gameplay data (no runtime
  fetches from `registry.*` or `api.*`).
- This site never becomes a social or community platform.

## Success criteria

The site is successful at v1 if:

- **Comprehension:** A first-time visitor can answer "what is this?"
  within ~5 seconds of landing (validated via clear hero + visible CTA
  above the fold).

- **Primary CTA ("Play now"):**
  - Visible above the fold on desktop AND mobile
  - Links directly to `play.legendary-arena.com`
  - Visually dominant versus secondary actions

- **Performance:**
  - LCP ≤ 1s on broadband desktop
  - Lighthouse ≥ 90 in Performance, Accessibility, Best Practices, SEO
  - No console errors in production

- **Cross-site consistency:**
  - Visual tokens match across www, play, registry
  - No visible style drift between properties at any given token version

- **Operational:**
  - A new blog post can be added and published in ≤ 10 minutes using
    the documented workflow

## Constraints

### Technical

- **Static-only** — Hugo + Cloudflare Pages. No SSR, no DB, no runtime
  API calls.
- **No build-time data fetching** — content is markdown; no fetching
  from the registry or the game server during build.
- **Brand token contract** — `static/brand-tokens.css`, hosted from
  this repo:
  - Public, versioned, cacheable asset
  - Backward-compatible within a major version
  - Treated as an external dependency by `play.*` and `registry.*`
  - Breaking changes require version increment AND coordinated consumer
    updates (per Global invariants)
- **Theme** — PaperMod **upstream as a Git submodule**:
  - Customized via Hugo overrides (`assets/`, `layouts/`)
  - **Not forked**
  - Custom theme deferred until proven necessary
- **Build contract:**
  - Hugo build must succeed locally and in Cloudflare Pages with
    identical output
  - No environment-specific behavior or configuration branches

### Operational

- **Solo-maintained** — content cadence and infrastructure must fit a
  single-operator pace.
- **Light governance** — this repo does not adopt the engine project's
  full WP/EC system. Vision plus `03-ROADMAP.md` is sufficient.
- **Brand identity TBD** — no LA brand book exists yet. WP-002
  establishes it before WP-003 (theme restyle) can begin.
- **Failure isolation** — this site must remain fully usable even if
  `play.*` or `registry.*` is offline. Cross-site links may fail, but
  rendering, navigation, and search must not break.

## Relationships to other projects

Each row describes the **direction of dependency** — what flows where,
and what doesn't.

| Project | Relationship |
|---|---|
| **Engine monorepo** (`legendary-arena/legendary-arena`) | Source of truth for the game. This site only **markets** the engine; no code or data flows back. |
| **arena-client** (`play.*`) | **Consumes** brand tokens from this site (cross-origin link + local fallback). **No runtime dependency** on www. |
| **registry-viewer** (`cards.legendary-arena.com`) | **Consumes** brand tokens from this site. **Owns all card data concerns**; this site never queries it. |
| **API server** (future `api.*`) | **No interaction** with this site, ever. |

## Change discipline

- Changes to the following require an update to this document AND an
  entry in the Decisions log:
  - Architecture (the three-site topology)
  - Scope (in / out / permanent non-goals)
  - Global invariants
  - Constraints (technical or operational)
  - Cross-site contract

- **Minor wording changes** (typos, clarifications that don't shift
  meaning) do not require log entries.

- Any **ambiguity** between this document and implementation must be
  **resolved before merge**. Ambiguity is a defect, not a feature.

- Conflicts between this document and `03-ROADMAP.md` are resolved by
  this document (vision is authoritative). Conflicts between this
  document and `.claude/CLAUDE.md` (project root) are resolved by
  CLAUDE.md (it's higher in the authority hierarchy).

## Decisions log

| Date | Decision | Why |
|---|---|---|
| 2026-08-09 | WP-042 locked — Cloudflare Pages **preview** deploys now build with `--baseURL "$CF_PAGES_URL"` so a branch's assets resolve on the preview host. **Problem:** `hugo.toml` `baseURL = https://www.legendary-arena.com/` + `canonifyURLs = true` bakes absolute production URLs into every build; any preview branch that changes a fingerprinted bundle (new CSS/JS → new hash) or adds processed images emitted `https://www.legendary-arena.com/…` URLs for files that exist only on the preview host → 404 → **unstyled page + broken images**. Previews therefore only rendered correctly for asset-*unchanged* branches; the gap was invisible until the first asset-changing branch. Surfaced by **WP-041** (art gallery): its preview rendered unstyled with an empty image grid even though every asset was built correctly and served fine from the preview host. **Fix:** `package.json` `build` script `hugo --minify && npx pagefind --site public` → `node scripts/build.mjs`. The wrapper appends `--baseURL "$CF_PAGES_URL"` **only** when `CF_PAGES_BRANCH` is set and ≠ `main`; on `main` and on local builds (no `CF_PAGES_*`) it runs the exact prior commands in the same order. CF Pages runs `npm ci && npm run build` verbatim (WP-006), so the fix is entirely repo-side — **no Cloudflare dashboard change**. **Determinism (amends the WP-006 build-command lock):** production output is unchanged by construction because the override is gated to non-`main` preview builds; verified two consecutive production builds byte-identical on `public/index.html` + `sitemap.xml`. **Preview path verified live:** WP-042's own preview served its stylesheet from `<hash>.legendary-arena-website.pages.dev` with **zero** `www.legendary-arena.com` asset refs, while genuinely-external nav links (Play, Cards) correctly stayed on their real domains. Wrapper is cross-platform (shell on Windows so the `hugo`/`npx` shims resolve; direct spawn on Cloudflare's Linux builders; no Node `DEP0190`). **Files:** `scripts/build.mjs` (new), `package.json` (build script). **Commit:** [`be53df7`](https://github.com/legendary-arena/legendary-arena-website/pull/91) — squash-merged via PR #91. | The first branch to change static assets (WP-041) exposed that preview deploys were non-functional for precisely the case previews exist to serve — reviewing visual/asset changes before they hit production. Fixing it in the versioned build script rather than the CF dashboard keeps the behavior reviewable and preserves WP-006's `npm ci && npm run build` parity contract. The determinism guarantee is what makes amending WP-006's "don't touch the build script" lock safe: `main` takes the identical code path it always did, so there is no production risk; the new behavior is scoped entirely to preview builds. Benefits every future asset-changing preview, not just WP-041 — which stays parked on its own PR until real artist art + display rights replace the placeholder seed bundles. |
| 2026-05-11 | WP-008 locked — SEO baseline + Schema.org markup live on `www.legendary-arena.com`. **Site-level meta-description rewrite at `hugo.toml` `[params].description`:** old value (verbatim, for audit) — `"A digital adaptation of the Legendary: A Marvel Deck Building Game."`; new value (verbatim, byte-identical to `content/_index.md` description front-matter; 152 chars) — `"Legendary Arena is a skill-first, web-based deck-building system. Assemble your heroes, face the scenario, and earn your standing."` The old string was the `strategy.md §10` "External IP dependency" failure mode in literal form AND was cascading via PaperMod's `head.html` lines 14-23 into every page's meta description, OG description, Twitter Card description, and home-page Schema description. Rewrite landed first in execution so it propagated through every downstream emission; mechanical grep confirms zero occurrences of "Marvel" in any `public/*.html` post-rewrite. **Schema.org partial at `layouts/_partials/seo/schema.html`** (new file, new subdirectory) emits the following page-kind matrix: `/` → `Organization` + `WebSite`; `/about/` → `AboutPage`; `/posts/` → `Blog`; `/posts/<slug>/` → (silent — PaperMod's `BlogPosting` + `BreadcrumbList` stand). Per-page gating uses **`.RelPermalink` strict equality** (NOT `.Section` / `.IsSection` / `.Type` — those would collapse `/posts/` and `/posts/<slug>/` into the same branch and leak `Blog` Schema onto every post; explicit Failure Condition in the WP body). **WebSite emits without `SearchAction`** — Pagefind is client-only with no URL-addressable `?q=` endpoint, so a fake SearchAction URL would 404 Google's verifier and likely suppress the entire WebSite entity. Decision locked at WP-008; revisit only via a future "Pagefind URL-addressable search" WP. **BlogPosting augment-not-replace:** PaperMod already emits a substantial `BlogPosting` shape on individual posts (headline, description, keywords, articleBody, wordCount, inLanguage, datePublished, dateModified, author, mainEntityOfPage, publisher); the project partial is silent on post pages rather than emitting a parallel BlogPosting. Step 2.3 image / logo contingencies did NOT fire (Rich Results passed on the launch post without a `BlogPosting.image` warning, and on home without an `Organization.logo` warning) — `static/images/og-default.png` and `static/images/logo-mark.png` placeholder files were not created. **PaperMod `templates/schema_json.html` override at `layouts/_partials/templates/schema_json.html`** is a tracking fork: Hugo lookup order resolves it ahead of `themes/PaperMod/layouts/_partials/templates/schema_json.html`; everything past the `else-if-(or-.IsPage-.IsSection)` marker is byte-identical to upstream (mechanical drift check at lock: 3170 chars identical, marker appears exactly once in each file). The `.IsHome` branch body is suppressed (replaced with a self-documenting comment) so the project partial owns the home-page Organization + WebSite shape without a duplicate-Organization de-dup tax in Rich Results. **Override maintenance contract (load-bearing survivability clause):** any future `themes/PaperMod` submodule update — whether driven by a dedicated theme-bump WP or a side-effect of an unrelated WP that pulls the submodule pointer forward — MUST re-run the Step 2.5 mechanical drift check before locking, to confirm (a) the upstream `.IsHome` branch is still structurally equivalent to what this override suppresses, and (b) no new required-for-Rich-Results properties were added to the upstream `.IsPage` / `.IsSection` branches that this override would now mask. The drift check is embedded in `docs/05-SEO-CONVENTIONS.md` "Override maintenance contract" section AND in a self-documenting comment block at the top of the override file itself, so a future theme-bump executor finds it without reading WP-008's body cold. **Schema-loader §4 block appended to `layouts/_partials/extend_head.html`** under the same production gate PaperMod uses for OG / Twitter / Schema partials (`hugo.IsProduction | or (eq site.Params.env "production")`); existing §1–§3 blocks (brand tokens link, Google Fonts `display=optional`, Pagefind lazy-load + keyboard shortcuts) are byte-identical pre/post. **`hugo.toml` `[params.schema]` block added** with `publisherType = "Organization"` and `sameAs = []` — empty array is intentional and recorded explicitly (no canonical social profile URLs to surface at v1); recording an empty array rather than omitting the key makes the gap explicit for the next author touching the map. **`docs/05-SEO-CONVENTIONS.md` committed** (~265 lines, 10 sections per WP body § Step 7). **`docs/04-CONTENT-CONVENTIONS.md` updated** — Status + Last-updated bumped; description-field row in the Front-matter table now links to `05-SEO-CONVENTIONS.md` (the "(prep for WP-008)" parenthetical removed); new "SEO surface" subsection under Production deploy points authors at `05-SEO-CONVENTIONS.md`. **External validator results on live URL:** (1) **Google Rich Results Test** clean on `/about/`, `/posts/`, and `/posts/2026-05-07-launch-announcement/` — Detected items + zero errors; (2) **Google Schema Markup Validator** clean on `/` (home) — zero errors; (3) **Twitter Card Validator** resolves card preview on both home + launch post (falls back to OG tags since PaperMod's `twitter_cards.html` requires a `[params].twitter` handle which is not configured at v1 — acceptable v1 posture); (4) **Google Search Console** property `https://www.legendary-arena.com/` (URL-prefix) added; ownership verified via **DNS TXT** record at apex `legendary-arena.com` with value `google-site-verification=1HyjlxNfv3Crv1NlyF6KUAAndTgyDDSZb13oIEEWQQA` (recorded verbatim here so a future operator can audit or re-issue without re-verifying); sitemap submitted at relative path `sitemap.xml` on 2026-05-11; GSC reports "Couldn't fetch" status immediately post-submission per the documented async lag (WP body § Step 6.4 explicitly anticipates this — lock proceeds on submission, not on async "Success" reading). **Lighthouse on live URL** (`https://www.legendary-arena.com/`, mobile preset, `npx lighthouse@12 --headless --no-sandbox --disable-gpu`): home P=92 / A=100 / BP=100 / SEO=**100**; launch post P=91 / A=100 / BP=100 / SEO=100. Home SEO clears the WP-008 tightened ≥ 95 gate with margin 5; all four categories ≥ 90 on both pages (min P=91 on post). Performance drift vs WP-011 lock pre-baseline (home 96, post 99) is within Lighthouse's typical ±3–8 point live-URL variance (network state, CF cache warmth, headless Chrome version) — pre-flight Risk #3 anticipated exactly this; the executor's fresh run IS the lock number per WP body § Step 8.8. Raw `lighthouse-home-wp008.json` / `lighthouse-post-wp008.json` outputs are local-only per WP-005 convention (not committed). **CF zone state confirmed unchanged at lock:** AI Crawl Control / Managed `robots.txt` = OFF (live `robots.txt` shows clean `User-agent: *` + `Disallow:` + `Sitemap:` line with NO `Content-Signal:` directive — the trip-wire for that setting); Browser Cache TTL = "Respect Existing Headers" (WP-006 CORS contract for `/brand-tokens.css` still firing with `max-age=3600`, `must-revalidate`, no `immutable`). Both still match the WP-006 lock-time state. **Four Option-D in-session amendments** folded into the WP body at lock per the WP-007a/WP-007b inline-amendment precedent (recorded against the WP body itself, not via a separate SPEC PR): (1) **§ Step 5 `robots.txt` regex-relaxed** — the WP body's expected-output snippet was `User-agent: *` only; actual Hugo emission is `User-agent: *` + `Disallow:` + `Sitemap: https://www.legendary-arena.com/sitemap.xml`. The `Sitemap:` line is correct + standard practice. Verification regex is "presence of `User-agent: *` AND absence of `Content-Signal:`", NOT strict equality. Pre-flight Caveat 1; documented at lock. (2) **§ Step 4 archetype no-op verified** — `archetypes/posts.md` line 4 already contained `description: ""` placeholder at draft time; the WP body's action item "If it doesn't, add the line and commit" collapses to a verification (confirm line present), not an edit. Pre-flight Caveat 2; documented at lock. (3) **§ Step 6.1 Rich Results Test vs Schema Markup Validator tooling split** — the WP body expected Google's Rich Results Test to report `Organization` + `WebSite` as detected items on the home page. Modern Rich Results Test only surfaces Schema types that trigger a SERP rich-result enhancement; `Organization` and `WebSite` (without `SearchAction`) are entity-disambiguation Schema and report "No items detected" with zero errors. This is correct behavior, not a regression. The right validator for home is the **Schema Markup Validator** at <https://validator.schema.org/> which validates structure for ALL Schema.org types. The WP body author conflated the two tools; amendment documents the split. (4) **§ Step 6.2 Facebook Sharing Debugger external-tool caveat** — the FB Sharing Debugger UI rendered as a blank page in the executor's session, likely an FB-side login / third-party-cookie / region condition (not our markup). Direct HTML inspection of the live URL confirms all `og:*` tags are present and correctly carry the post-rewrite brand-clean copy (`og:title`, `og:description`, `og:url`, `og:type` = `website` on home and `article` on post, `og:site_name`, `og:locale`). Same shape as WP-007a's recorded "browser-extension blocking" observation — the contract is satisfied via direct inspection; the external tool being unusable is not a contract failure. **Mechanical reproducibility, submodule, scope-lock:** two consecutive `npm run build` produce byte-identical `public/` across 53 files (SHA-256 `Compare-Object` empty); `themes/PaperMod` clean at `c4ca7ca486ecd67c8f6bba31551a6ee0d1455926` with no `+` flag throughout; `git diff --name-only main...HEAD` at implementation-commit time returned exactly 6 paths, all within the WP-008 Allowed list (`docs/05-SEO-CONVENTIONS.md`, `docs/ai/work-packets/WP-008-seo-baseline.md`, `hugo.toml`, `layouts/_partials/extend_head.html`, `layouts/_partials/seo/schema.html`, `layouts/_partials/templates/schema_json.html`). **Commits:** `27383e7` (SPEC: WP body), `5c8fd25` (WP-008: implement) — both squash-merged to `main` as `49ce5c5` via [PR #9](https://github.com/legendary-arena/legendary-arena-website/pull/9); plus this lock commit. **Pre-flight artifact** at `docs/ai/invocations/preflight-wp008-seo-baseline.md` + the session prompt at `session-wp008-seo-baseline.md` are scratchpads (gitignored per repo convention — matches WP-010 / WP-011 precedent); verdict was **READY TO EXECUTE** 2026-05-11 with 2 recorded caveats (folded into lock per items 1+2 above), 13/13 Known Failure Modes PASS. | Final marketing-side WP that completes the v1 SEO posture. Three pre-existing rules that load-bore the lock: (a) the brand-failure-mode rewrite at Step 1.4 closes the contradiction between WP-007b's "Brand artifacts → v1 LOCKED" entry and PaperMod's site-wide meta-description fallback containing a `strategy.md §10` failure mode — leaving the old copy at lock would have meant WP-008 signed off on a live-site SEO surface carrying a documented brand failure mode; (b) the `.RelPermalink` strict-equality decision is the single most load-bearing template invariant — a future "cleanup" pass swapping it for `.Section` / `.IsSection` would silently leak `Blog` + `BreadcrumbList` Schema onto every individual post and trip the Rich Results de-dup tax; the docstring at the partial head + the failure-condition entry + the §05-SEO-CONVENTIONS.md "Per-page gating uses `.RelPermalink` equality" section together are three layers of redundant documentation so the rule survives turnover; (c) the PaperMod override maintenance contract is the survivability clause for the next theme-bump executor — without it, a future submodule update could silently mask new upstream Rich Results properties or move home-page Schema out from under the override. Recording the contract here, in the override comment block, AND in `05-SEO-CONVENTIONS.md` is the same belt-and-suspenders pattern that made WP-006's CF zone state survivable. Four Option-D amendments at lock continues the WP-007a/WP-007b precedent: spec-vs-reality drift surfaced during execution gets corrected inline rather than via a separate SPEC PR, with each amendment recorded against the WP body + this Decisions log. Four is within the soft ceiling that hardening passes typically catch; the two pre-flight caveats (robots.txt regex + archetype no-op) were already recorded at pre-flight time, so only the Rich Results vs Schema Markup Validator tool clarification and the FB Debugger blank-page external-tool caveat were truly new at execution. The Rich Results tool clarification is the most novel of the four — it documents a real Google tooling distinction that the WP author conflated, and it shifts a future reader's expectation from "Rich Results will show Organization + WebSite" to "Rich Results only flags rich-result-eligible types; use Schema Markup Validator for everything else." The FB Debugger caveat is analogous to WP-007a's browser-extension blocking note — an external tool's behavior is not the contract; the contract is what the live HTML actually serves, and that was verified via direct probe. **Downstream unblocking:** WP-009 (cross-site class-color usage audit) is now the only outstanding marketing-side v1 WP; WP-008 and WP-009 have no shared write paths so the order they execute in is interchangeable from a dependency standpoint, but WP-009 reads WP-008's templates as part of its audit surface so executing WP-009 after WP-008 means it sees the locked Schema posture rather than the pre-Schema state. Search Console "Couldn't fetch" status is expected to flip to "Success" within 24h per Google's documented async sitemap-processing lag; if it doesn't (e.g., a CF cache or DNS issue surfaces), the trip-wire is the same as Step 8.4 (live `sitemap.xml` reachable + valid XML + lists the four required URLs) — both already verified at lock. |
| 2026-05-11 | WP-007b locked — `cards.barefootbetters.com` consumes brand tokens v1 cross-origin from `www.legendary-arena.com/brand-tokens.css` with bundled local fallback. **v1 cross-site carve-out is now FULLY CLOSED** (WP-007a closed the `play.*` half on 2026-05-10; WP-007b closes the `cards.*` half). Brand artifacts move from `v1 LOCKED for WWW; cross-site consumption pending WP-007b` to **v1 LOCKED**. **CF Pages project (cards):** `legendary-arena` (existing; no new project; no DNS change; refresh-only deploy via push to `main`). **Hostname posture preserved:** registry stays at `cards.barefootbetters.com` for v1 per the 2026-05-07 Decisions log; the migration to `cards.legendary-arena.com` remains a deferred separate WP. `docs/ops/domains.json` / `docs/ops/DOMAINS.md` untouched. **SHA-256 hash parity at lock:** `70C11CEB75A993F2806056DB8D955D5D3133362D97C03A51EFB6719C575713FF` — byte-identical to WP-007a's 2026-05-10 lock-time hash. Same v1 contract for both `play.*` and `cards.*` consumers; future v1 → v2 brand-tokens bump must refresh both consumers' bundled fallbacks in lockstep. **Engine-repo commits:** [`f62ddef`](https://github.com/barefootbetters/legendary-arena/pull/36) (`EC-154: cards.barefootbetters.com brand integration`); [`d18bc91`](https://github.com/barefootbetters/legendary-arena/pull/37) (`EC-155: registry-viewer SEO errata (WP-007b)`). EC slot retargeted from natural `EC-007b` to `EC-154` per the locked collision-numbering rule (same precedent as WP-007a's `EC-007a` → `EC-146`). **Lighthouse on `https://cards.barefootbetters.com/` (post-EC-155 deploy):** Performance 61 / Accessibility 95 / Best Practices 79 / SEO 100. SEO and Accessibility clear the ≥ 90 floor; **Performance and Best Practices are explicitly carved out per WP body Amendment 7** — root causes are pre-existing registry-viewer characteristics (R2 data-pipeline cost on initial load: sets index + per-set JSON + themes + glossary + card types + abilities all fetched before render; existing UI font sizes; existing library API deprecations). These predate WP-007b and were present on the unbranded pre-merge URL at substantially the same scores; fixing them requires structural changes (lazy R2 fetches, code splitting, UI font audit, library deprecation cleanup) out of scope for a brand-integration WP. Deferred to a future "registry-viewer performance + Best Practices optimization" WP. **Seven Option-D in-session amendments** were folded into the marketing-repo execution PR per [`01.0b §"When parallel mode is more trouble than it's worth"`](docs/ai/REFERENCE/01.4-pre-flight-invocation.md) (which explicitly names WP-007b as the canonical case for sequencing SPEC + execution against the same authority): (1) `--la-font-sans` → `--la-font-body` typo (5 occurrences across Step 3 pre-check + index.html template + prose + §Forbidden CSS transformations reference; the v1 contract has `--la-font-body` for Inter, not `--la-font-sans`); (2) §Step 5 class-color section rewritten — the actual class-color render surface is `HC_COLOR` in `apps/registry-viewer/src/lib/theme.ts` (consumed by `CardGrid` / `CardDetail` for class badges via inline `:style` bindings per `palette.md` §4.4 application patterns), not the `filterHC` `<select>` (which is a native `<option>` element that can't host class-color styling); (3) §Step 5 gains "Mount-point coupling (in-scope)" sub-section covering two unavoidable couplings the mount deliverable creates — `.app { height: 100vh }` → `flex: 1; min-height: 0` and the single `App.vue` → `AppShell` mount-coupling import; §Execution mode mechanical check 1 wording amended in parallel ("no net-new *data-pipeline* imports outside `branding/`" rather than "no net-new imports outside `branding/`"); option B (`AppShell.vue` wrapper) adopted; (4) build-command observation matching WP-144 / D-14401 — local single-package `pnpm --filter registry-viewer build` fails on a fresh worktree (workspace-dep build cache miss); topological form `pnpm --filter "registry-viewer..." build` works. PR-time CF Pages preview deploy on the cards project succeeded with the locked text, confirming the existing project's command handles workspace deps correctly out of the box (likely via topology-filter or `pnpm -r build` prefix); locked-command text in §Step 1 / Assumptions / §Step 8 not changed pending CF dashboard visibility; (5) §Step 7 byte-budget restructured into snapshot vs implementation split — +10 kB budget applies to implementation bytes only (everything except `dist/brand-tokens.local.css`); snapshot bytes are sized by the v1 contract, reported separately, gated only by SHA-256 hash-parity at Step 4 + Step 9.3. This WP's implementation-byte delta = +8,078 (within budget); snapshot bytes = +10,799; total dist bytes = 256,303 → 275,180; file count 4 → 5; (6) engine-repo EC file retraction — the session prompt claim "no separate engine-repo WP file (this WP IS the contract)" was incomplete; the engine repo's CI step "Verify code commits reference ECs" requires every commit touching `apps/**` or `packages/**` to use an `EC-NNN:` subject prefix AND have a matching `docs/ai/execution-checklists/EC-NNN-*.checklist.md` file. The validator regex `^EC-[0-9]+[A-Z]?:` also rejects lowercase suffixes (`EC-007b:` would fail even with the file). EC slot retargeted to `EC-154` per the locked collision-numbering rule; (7) Lighthouse gate carve-out — Performance + Best Practices failures pre-existing, deferred to a future optimization WP; SEO failures closed by EC-155 (same root cause + fix pattern as WP-007a's EC-148, 2026-05-10). | Second real cross-origin consumer of the WP-002 v1 brand-tokens contract goes live; the contract is now exercised on both planned consumer surfaces (`play.*` and `cards.*`). The Option-D fold pattern continues the WP-007a precedent: spec-vs-reality drift surfaced during execution gets corrected inline rather than via a separate SPEC PR, with each amendment recorded against the WP body and the rationale captured in this Decisions log. Seven amendments is a higher count than WP-007a's two — well past the "soft ceiling of ~3 audit findings" hardening passes typically catch — and is itself a quality signal worth recording: hardening passes against WP bodies appear to have diminishing returns past a few rounds; further drift gets surfaced only by actual execution. The hardening pass at PR #4 (commit `4433747`) caught the strict-integration-mode invariant, the mechanical 4-point enforcement check, the Allowed/Forbidden CSS transformations list, the Header/Footer import contract grep, the token-existence pre-check, the robust SNAPSHOT regex, the bundle-size baseline definition, the fail-fast Step 9 ordering, and the no-silent-fixes rule — substantial value — but missed the seven gaps surfaced during execution. The Amendment 7 carve-out is the most novel of the seven: it narrows a §Failure Conditions absolute gate (Lighthouse ≥ 90 in all four) to a delta-attributable gate, on the recognition that a refresh-only WP cannot be responsible for pre-existing characteristics of the deployment it's refreshing. WP-007a's analog (EC-148) was similar in shape (SEO fix) but applied to a NEW deployment, where the same audits failing would have been WP-007a's responsibility; for WP-007b, the unbranded pre-merge URL already had the same Perf/BP scores, so the fix-or-defer question is different. Honest carve-out vs scope-expansion vs not-locking is the right framing. Both WP-009 (cross-site class-color audit) and any future v1 → v2 brand-tokens migration are now unblocked: WP-009 can run against fully-integrated consumers on both `play.*` and `cards.*`; v2 migrations have two real consumers to coordinate. |
| 2026-05-10 | WP-007a locked — `play.legendary-arena.com` is live. CF Pages project: **`legendary-arena-play`** (`legendary-arena-play.pages.dev` auto-hostname; `play.legendary-arena.com` custom-domain bound + cert Active 2026-05-10). Lighthouse on the live URL: **97 / 100 / 100 / 100** (Performance / Accessibility / Best Practices / SEO). Pinned **`NODE_VERSION = 22`** as the only CF Pages env var (pnpm version auto-detected from root `package.json` `packageManager: pnpm@10.32.1`); plus **`VITE_SERVER_URL = https://api.legendary-arena.com`** (Production scope) added during execution to satisfy the arena-client source contract at `apps/arena-client/src/lobby/lobbyApi.ts:14-21`. **Pre-WP-007a DNS state for `play.legendary-arena.com`** (rollback baseline): no public records on the `legendary-arena.com` Cloudflare zone; `Resolve-DnsName` returned `DNS name does not exist`. WP-007a added a CNAME `play` → `legendary-arena-play.pages.dev` (Proxied) at Step 10 when CF Pages prompted. **SHA-256 hash-parity contract codified:** the live `https://www.legendary-arena.com/brand-tokens.css` body and the bundled fallback at `apps/arena-client/public/brand-tokens.local.css` (with the SNAPSHOT comment block stripped for comparison) MUST hash byte-identical. Lock-time hash: `70C11CEB75A993F2806056DB8D955D5D3133362D97C03A51EFB6719C575713FF`. Future v1 → v2 brand-tokens bump must refresh the bundled fallback in lockstep with the live URL update; mismatch is the trip-wire that catches stale snapshots before consumers diverge. **v1 cross-site carve-out: HALF-CLOSED** at WP-007a lock; full closure when WP-007b (registry-viewer brand integration) also locks. Until then, brand artifacts remain `v1 LOCKED for WWW; cross-site consumption pending WP-007b` in the marketing-repo `docs/brand/CHANGELOG.md` and palette/typography/spacing.md status lines. **Two execution-time errata closed inline** (engine-side ECs in the engine repo; this WP is the originating contract per engine-repo `.claude/rules/work-packets.md`): (1) **EC-147** — `apps/server/src/server.mjs` boardgame.io `Server({ origins: [...] })` allowlist amended to include `https://play.legendary-arena.com` and `https://legendary-arena-play.pages.dev`. Surfaced during WP-007a Step 9: `https://api.legendary-arena.com/games/legendary-arena` returned 200 with no `Access-Control-Allow-Origin` header to a probe from `https://legendary-arena-play.pages.dev` Origin (boardgame.io `Server`'s cors-package behavior when the request Origin is not in the allowlist). The original WP-007a Step 7 env-var lock missed `VITE_SERVER_URL`; both gaps closed with the env-var add + EC-147 server-side change. Branch-prefixed CF Pages preview-deploy hostnames (`<branch>.legendary-arena-play.pages.dev`) are NOT in the allowlist (cors-package allowlist matching is exact-string; no wildcard support); preview-deploy lobby fetch failure is small blast-radius (short-lived URLs; brand-integration visual check on a preview deploy doesn't depend on lobby API) and is scope-deferred. (2) **EC-148** — `apps/arena-client/index.html` gained a `<meta name="description" content="Legendary Arena — the digital deck-building arena. The arena awaits." />` line (copy aligns with the WP-004 home-page tagline lock); `apps/arena-client/public/robots.txt` (new) ships a verbatim two-line allow-all (`User-agent: *\nDisallow:`). Surfaced during WP-007a Step 11.5 Lighthouse: SEO scored 82 because `index.html` had no meta description and `/robots.txt` was being shadowed by CF Pages's default SPA fallback (returning the SPA shell HTML; Lighthouse parsed it as a robots.txt and found 37 errors). After fix, both audits flipped 0→1 → SEO=100. **Three additional execution-time observations recorded** (not contract gaps; documented for future executors): (a) **CF Pages `_redirects` lint quirk** — `/*  /index.html  200` is rejected by CF Pages's lint as "infinite loop detected" and the rule is ignored at deploy time. CF's default SPA fallback covers the deep-path use case (verified by Step 9 deep-path probe returning 200 + SPA shell HTML), so the WP-007a Step 8-prescribed `_redirects` file is functionally inert in CF Pages. The file remains in the bundle per the Step 8 contract; a future INFRA WP can either remove it or convert to a syntax CF accepts. (b) **Cross-platform line-ending observation** — Windows `core.autocrlf=true` converts source files to CRLF on checkout; CF Pages clones on Linux with LF. Bundle bytes diverge between local-Windows and CF-Linux builds (Vue scoped-CSS hashes encode source content at compile time, including line endings; lazy-chunk filenames cascade from those). Functional + visual render parity preserved (same DOM, same styles, same routing); per-machine determinism (Step 6 contract) holds. Cross-machine bundle byte-determinism would require a `.gitattributes` `* text=auto eol=lf` change + `git add --renormalize` — wider scope than WP-007a, recurring across the engine repo, deferred to a future INFRA WP. The Step 9 build-parity assertion's "render identically" wording (not "byte-identical") is satisfied. (c) **Browser-extension cross-origin blocking** — privacy / tracking extensions on some user browser profiles (uBlock Origin with strict lists, Privacy Badger, NoScript, etc.) can synthetically block cross-origin asset fetches and log `(index):30 GET https://www.legendary-arena.com/brand-tokens.css net::ERR_ABORTED 403 (Forbidden)` in the user's DevTools Console. This is browser/extension-specific, not a CF or server contract failure: server-side probes from synthetic origins all return 200 + ACAO=*. Verified in incognito as the canonical clean-browser state. This IS exactly the failure mode the bundled local fallback is designed to handle — the cascade contract makes the live-URL outage visually invisible (`<link>.disabled = true` test confirms before/after computed `--la-*` values byte-identical). **Server-side follow-up note (NOT in WP-007a scope, surfaced for tracking):** `apps/server`'s `PUBLIC_BASE_URL` env var should be updated to `https://play.legendary-arena.com` so Stripe Checkout success/cancel redirects land on the live arena-client. Hanko allowed origins should include `play.legendary-arena.com`. Stripe webhook redirect allowlist similar. All three are server-side env / dashboard config changes, owned by a separate server-config or billing-flow WP. Tracked in engine-repo `docs/ops/DOMAINS.md` Cutover step 5 as still-pending. | First real cross-origin consumer of the WP-002 v1 brand-tokens contract goes live; the contract moves from "theoretical" to "load-bearing." Two execution-time errata pattern continues the WP-006 → WP-007a precedent of "design correct on paper, reality surfaces gaps that need surgical inline fixes" — same as the WP-006 `_redirects` discovery. Each fix landed with its own EC + governance trail (EC-147 + EC-148 in the engine repo) so future readers can see what surfaced when. The half-closed cross-site carve-out is honest reporting: WP-007a verifies one half of the contract (play consumes www's tokens correctly); WP-007b will verify the other half (registry consumes the same tokens correctly). The three observation items (CF lint quirk, line-ending divergence, extension blocking) are each "known characteristic, not a regression" — recording them now prevents a future executor from rediscovering the same diagnostics from scratch. The browser-extension blocking observation is particularly load-bearing for the cascade contract's reason-to-exist: this is the failure mode the bundled fallback was designed for, and it surfaces in the wild more often than network outages. Verifying the cascade contract under that exact stress (via `<link>.disabled = true` in DevTools) is the correct way to certify the fallback works in production conditions. |
| 2026-05-07 | Separate repo from engine monorepo | Hugo content has different governance needs than engine code |
| 2026-05-07 | Subdomains over path-based hosting | Independent deploys, independent caching, cleaner failure isolation |
| 2026-05-07 | PaperMod upstream submodule + Hugo overrides (over fork or custom-thin theme) | Faster v1; overrides keep the theme upgradeable indefinitely; revised from earlier "fork" plan during WP-001 execution |
| 2026-05-07 | Pagefind on Hugo content only | registry.* has its own structured search; no duplication |
| 2026-05-07 | Wordmark placeholder for logo | Real logo design deferred to a later effort, possibly with a contractor |
| 2026-05-07 | New `legendary-arena` GitHub org | Brand consistency; org transferable later if circumstances change |
| 2026-05-07 | Brand tokens hosted from this repo's `static/` as a versioned API contract | Single SoT consumed cross-origin by play.* and registry.*; treating as a contract prevents silent cross-site breakage |
| 2026-05-07 | Strengthened vision: Global invariants, failure isolation, cross-site contract, change discipline, measurable success criteria | Convert implicit philosophy into enforceable rules; tighten cross-site guarantees; eliminate ambiguity (esp. theme + tokens) |
| 2026-05-07 | Added SEO baseline to v1 scope (WP-008) | RankMath-equivalent SEO is a baseline expectation for a public marketing site; making it explicit prevents it from being skipped or reinvented mid-project |
| 2026-05-07 | Registry remains at `cards.barefootbetters.com` for v1; migration to `registry.legendary-arena.com` deferred to a future scoped effort | Reality recognized: the registry already exists at cards.* and migration would be a substantial coordinated change. Pinning v1 on a non-blocking domain change adds risk without proportionate benefit. |
| 2026-05-07 | Karpathy LLM-wiki pattern evaluated for www; rejected. Engineering wiki spawned in engine project. Player wiki (if needed) → registry.* (per ER-016). | Marketing site scope must remain conversion-focused; wiki content would violate permanent non-goals on card/gameplay data. Reference material belongs with reference material (registry); engineering knowledge belongs with engineering code (engine project). |
| 2026-05-07 | WP-002 + WP-003 locked — brand v1 verified at www.legendary-arena.com (Lighthouse 91/100/100/100, both modes, mobile 375×667, all `palette.md §8` contrast pairs WCAG AA). Brand artifacts move from "v1 DRAFT" to "v1 LOCKED for WWW." | All Phase B exit criteria from `strategy.md §11` met except cross-site consumption (deferred to WP-007a/b). Verification surfaced and resolved a silent dark-mode selector bug, a touch-target gap, a footer contrast violation, missing favicons, and the need for an additive `--la-color-cta` token to satisfy white-on-CTA AA in dark mode — all documented in `docs/brand/CHANGELOG.md`. |
| 2026-05-07 | `docs/ai/work-packets/` adopted for session-ready WP execution prompts (not WP design specs) | Roadmap remains the design source of truth; these files are the "do this" pack a fresh Claude session reads to execute a WP. Same directory name as the engine project but lighter purpose — does NOT adopt the engine's full WP/EC governance system. First instance: `WP-004-content-scaffolding.md`. |
| 2026-05-08 | Added §9.1 "Early Lock Revision Window" exception clause to `palette.md` governance — codifies a 24–72h post-lock redefinition allowance with explicit entry conditions, duration, exit trigger, and post-close enforcement. Window for v1 lock is OPEN through **2026-05-10 23:59 local**, OR until any downstream consumer integrates the locked tokens, whichever comes first. After close, future redefinitions require formal v1 → v2 bump per §9 strict. | Allows brand iteration immediately post-lock without paying the v2 coordination cost, while preserving rule authority and audit trail. The contract amendment converts what would otherwise be a silent contract violation into a documented, time-bounded exception. The clause persists in the doc as precedent for future v(n) lock periods but does NOT auto-renew — each new major version's window must be opened explicitly in `palette.md §12`. |
| 2026-05-08 | Brand tonal shift performed under §9.1 window. Brand red moved cherry → **deep maroon** (`#7a1d1f` light / `#a83034` dark). Brand blue moved royal → **deep navy** (`#1e3a8a` light / `#3753b8` dark) — Captain America / X-Men Beast tonal territory. CTA tokens follow new maroon (~10.4:1 contrast on white text — AAA, well above the previous 5.44:1). All bright/muted variants of red and blue scaled accordingly. Gold unchanged. Class colors and semantic colors (success/warning/error) unchanged. Companion governance refinements: `palette.md §5.3` distinguishing rule (error red MUST stay in pure-red family; CTA may shift toward maroon), `§4.3` role discipline (`--la-color-blue-bright` MUST carry interactive affordance), `§7` gradient tonal note (`--la-gradient-hero` now spans navy → maroon), `§10` failure-mode bullet for blue affordance misuse. | Visual identity shifts from "energetic SaaS" to "muted cinematic" — better aligned with the mythic / Marvel-adjacent brand position. Per §9.1, treated as v1 revision rather than v2 bump (no consumer integration yet at lock time). Verified end-to-end via Hugo serving the new tokens and `getComputedStyle` resolving them in both modes. |
| 2026-05-08 | Class-color subsystem added at `palette.md §4.4` — 10 mode-stable tokens for the 5 hero classes (`strength`, `covert`, `instinct`, `ranged`, `tech`) with bright + muted pairs each. Sourced from production class icons in `barefootbetters-legendary-setup/public/img/icons/hero-classes/`. Two muted variants (`instinct-muted` `#92400e`, `ranged-muted` `#155e75`) are *derived* (Tailwind amber-800 / cyan-800) since their source icons lack shadow companions. Class colors live one layer below brand identity (§4.1–§4.3) and parallel to gameplay mapping (§6); they identify *gameplay role*, not *product identity*. Usage patterns documented (border-accent, chip-fill, icon-accent, selection-state). Failure mode added to `§10`. | Decision-by-elimination: a desired green for mythic / forest / cleric narrative could have been added as a 4th brand color, but doing so would have collapsed the brand-color hierarchy (gold-led with red+blue accents) and collided semantically with `--la-color-success`. Treating class colors as a parallel subsystem keeps brand restrained at three colors while enabling gameplay expressiveness across ten tokens. Brand = stable + small palette; gameplay = expressive + larger palette. That asymmetry is intentional. |
| 2026-05-08 | Logo work moved from "deferred placeholder" to "design contract + designer handoff." Artifacts: `docs/brand/logo-brief.md` (acceptance criteria, locked constraints, accessibility), `docs/brand/logo-ai-workflow.md` (evaluation rubric + exploration record + designer-handoff thesis), `docs/brand/logo-explorations/` (8 directions × 6 sizes archived — 4 monogram directions + 4 abstract-symbol directions, each rendered at 16/32/64/128/256/512px), `docs/brand/logo-figma/` (4 SVG variants from a Figma export — icon-only, light-mode lockup, dark-mode lockup, alternating-color hexagon test). Conclusion of AI iteration: monogram round was "letters in proximity, not real monograms"; abstract round split into "form discipline (B Summit) vs product fit (D Hand) without fusion." Designer handoff thesis is **"D done with B's discipline"** — start from D, apply B's silhouette filter; cards form the silhouette, peak emerges from convergence rather than being imposed on top. Wordmark placeholder remains active on `www.legendary-arena.com` until a logo passes `logo-brief.md §9` acceptance. | The logo TBD entry was abstract — now it has a concrete spec, named failure modes (stratified read, pivot blob, wedge collapse, letter mashup), a named candidate direction, and an explicit handoff. Anyone (designer, contractor, future-self) can pick this up cold from the brief + workflow notes + exploration archive. AI iteration ceiling reached on this problem; further progress requires Figma optical-balance work that LLM SVG generation can't reliably produce. |
| 2026-05-08 | WP-004 locked (content scaffolding + first 3 pages). Home page (hero "The arena awaits." + three "why" cards + "Play now" CTA), about page, blog post "Opening the arena", `archetypes/posts.md`, and `docs/04-CONTENT-CONVENTIONS.md` shipped. Home-page-layout choice: **Approach A — override `layouts/index.html`**. The override hosts the `.hero` block + `.button` CTA above the fold (PaperMod's `homeInfoParams` partial cannot host a styled CTA, ruling out approach C; a Hugo shortcode invoked from `content/_index.md` — approach B — adds an indirection layer without ergonomic gain). Hugo's lookup resolves `layouts/index.html` ahead of the theme's `list.html` for the home kind; PaperMod's `baseof.html` still applies `body class="list"` to `.Kind == "home"` so `custom.css §1`'s `body.list` background anchor continues to apply. Copy (`title`, hero prose, `ctaLabel`, `ctaHref`, `sections[]`) lives in `content/_index.md` front-matter so authors can edit text without touching layout. Lighthouse on production build: home 97/95/96/100, post 98/96/96/100. Binary CTA test PASS in all four viewport+mode combinations (light/dark × 1280×800/375×667). The `[params.homeInfoParams]` block in `hugo.toml` (the WP-001 placeholder mechanism) was removed during the WP-004 tidy-up pass; a comment in `hugo.toml` points future readers to `docs/04-CONTENT-CONVENTIONS.md`. | The home-page-markup choice is a structural decision future contributors need to discover quickly when editing the home page. Recording it here (and in `docs/04-CONTENT-CONVENTIONS.md "Home page markup"`) prevents the next person from re-deriving the constraint or — worse — replacing the override with a `homeInfoParams` change that silently strips the CTA. The override pattern keeps the PaperMod theme submodule clean (no fork, `git submodule status` unchanged), satisfies `01-VISION.md §Success criteria → Primary CTA` directly, and follows the same principle as WP-003's `assets/css/extended/custom.css` overrides: customise via Hugo's lookup order, not by modifying upstream. |
| 2026-05-09 | WP-005 locked (Pagefind search integration). Build pipeline established: npm route, `pagefind` 1.5.2 exact-pinned in `package.json` + committed `package-lock.json`, single build command `npm ci && npm run build` resolving to `hugo --minify && npx pagefind --site public`. Pagefind UI: default UI lazy-loaded on first interaction (focus stub, click `#la-search`, or press `/` / `Ctrl+K` / `Cmd+K`) with a server-rendered stub `<input id="la-search-stub">` for immediate affordance — eager `defer` loading consumed enough TBT to push Performance to 88-89 on home, below the ≥ 90 floor. PaperMod's built-in Fuse.js search disabled via `[outputs] home = ["HTML", "RSS", "JSON"]` removal from `hugo.toml` (no `content/search.md` existed; PaperMod template untouched in submodule). Index scope structural via `data-pagefind-body` on `<main>` in `layouts/baseof.html` override; header/footer/nav naturally excluded. Keyboard policy: `/` and `Ctrl+K` / `Cmd+K` focus the search input; bail when typing in any input EXCEPT the stub itself; preventDefault on focus path. Lighthouse on production build (served at `localhost:1314` with `--baseURL http://127.0.0.1:1314` for localhost DNS parity): home 92/100/100/100, post 93/100/100/100. Reproducibility verified mechanically (two consecutive builds, `Compare-Object` over SHA-256 hashes returned empty). Submodule clean. The single-command contract is the WP-006 deliverable (Cloudflare Pages will run the same `npm ci && npm run build` verbatim). | Search is the last load-bearing piece before the marketing site can deploy. The exact-pin + reproducibility-check discipline collapses the largest determinism risk WP-006 inherits from a CI build — silent dependency drift between local and CI was the failure mode WP-005 most needed to prevent. The lazy-load pattern is a new structural decision: future search-related work must preserve the "stub renders eagerly, real UI loads on intent" contract or the Performance budget will silently regress. Pagefind version bumps are governed work, not casual updates — a separate WP must re-run the mechanical reproducibility check and re-verify Lighthouse ≥ 90 before locking, per WP-005 Constraints. |
| 2026-05-09 | WP-006 locked (Cloudflare Pages deploy + custom domain). Site live at `https://www.legendary-arena.com` (canonical) + `https://legendary-arena.com` 301-redirecting to www (path-preserved). **Apex-redirect mechanism amendment:** the WP-006 Step 6 locked decision specified `static/_redirects` (option A) as REQUIRED and PROHIBITED CF dashboard rules (option B). During execution this turned out to be technically incorrect — CF Pages' `_redirects` engine does NOT support full-URL source patterns (only path-only). The committed `static/_redirects` file (commit `f397807`) was silently ignored; apex served duplicate content. Mechanism amended to a zone-level **Cloudflare Redirect Rule** on the `legendary-arena.com` zone (Rules → Redirect Rules), created from CF's "Redirect from root to WWW" template — wildcard source `https://legendary-arena.com/*`, dynamic target `https://www.legendary-arena.com/${1}`, 301 permanent, preserve query string. The no-op `static/_redirects` was reverted in PR #1 (squash-merged as `3871d7d`). The in-repo reproducibility argument that originally motivated rejecting option B is preserved in spirit: zone Redirect Rules are exportable via Wrangler CLI or Terraform; a future WP can land a config-as-code artifact for the rule if reproducibility becomes load-bearing for cross-site contracts. **CORS posture for `/brand-tokens.css`:** `Access-Control-Allow-Origin: *` + `Cache-Control: public, max-age=3600, must-revalidate` shipped via in-repo `static/_headers` (commit `3c955a8`). Locked posture encodes the brand-tokens v1 contract commitment — single URL, version-in-body. Consumers fetch `/brand-tokens.css` and read the `Version: v1` header comment to verify the contract; v1 → v2 is a coordinated consumer swap on the SAME URL, not filename-versioned URLs (`brand-tokens.v1.css`). `immutable` was rejected because it would silently misalign with that contract on a future v2 bump. 1-hour TTL trades a small egress cost for sub-coffee-break propagation under the unversioned single-URL v1 model. A future WP that proposes filename-versioned brand-tokens is renegotiating WP-002's contract and must update this posture (or move to `immutable`) at the same time. **Two CF zone-level settings recorded as load-bearing for the contracts above to actually fire:** (1) Caching → Configuration → **Browser Cache TTL: "Respect Existing Headers"** — required so origin Cache-Control survives to clients (CF's default 4-hour zone TTL was overriding `static/_headers` and serving `max-age=14400` instead of `3600`); (2) **AI Crawl Control / Managed robots.txt: OFF** — kept Hugo's clean `User-agent: *` `robots.txt` rather than CF-injected version with `Content-Signal:` directive that Lighthouse v12 doesn't recognize (cosmetic SEO regression 100→92 was caused by this; `Content-Signal:` is a valid newer AI-bot signal but not yet in Lighthouse's recognized-directive list). **Pinned env vars in CF Pages:** `HUGO_VERSION=0.161.1` (matches `README.md` Prerequisites and theme `module.toml` floor of 0.146.0) and `NODE_VERSION=22` (Node major from README; CF resolves to `22.22.0`). **Pre-WP-006 DNS state on the `legendary-arena.com` Cloudflare zone (rollback baseline):** zone was already on Cloudflare DNS with authoritative NS `lakas.ns.cloudflare.com` / `teagan.ns.cloudflare.com`; no public records on the apex; `www`, `api`, and `ewiki` CNAMEs already existed alongside Namecheap email-forwarding `MX` records (eforward1-5.registrar-servers.com priorities 10/10/10/15/20) and the matching SPF `TXT` (`v=spf1 include:spf.efwd.registrar-servers.com ~all`). WP-006 added the apex CNAME (`legendary-arena.com` → `legendary-arena-website.pages.dev`, Proxied) when CF Pages prompted for it during custom-domain bind. **CF Pages project name:** `legendary-arena-website` → `legendary-arena-website.pages.dev`. **Lighthouse against live URL (post-zone-fixes):** home P=97 / A=100 / BP=100 / SEO=100; post P=99 / A=100 / BP=100 / SEO=100. All four categories ≥ 90 with margin. Console errors: 0 items on both pages. Internal link sweep: 16 unique hrefs, all 200. Reproducibility: two consecutive `npm ci && npm run build` runs produce byte-identical `public/` per `Compare-Object` over SHA-256 hashes (empty diff). Submodule clean: `c4ca7ca486ecd67c8f6bba31551a6ee0d1455926 themes/PaperMod (heads/master)` with no `+` flag. **Commits:** `3c955a8` (Step 4 `_headers`), `f397807` (Step 6 first attempt — superseded), `3871d7d` (PR #1 squash: amendment + Step 7 verification), and the WP-006 lock commit itself. **Invocation scratchpads** under `docs/ai/invocations/` (`context-wp006-pre-execution-notes.md`, `preflight-wp006.md`, `session-wp006-cloudflare-deploy.md`) remain untracked per project convention — they helped this session but don't need to live in history. The WP-006 design pack at `docs/ai/work-packets/WP-006-cloudflare-deploy.md` IS committed (matching WP-005's pre-flight-artifacts pattern), with a post-execution amendment section pointing to this Decisions log entry. | First production deploy of the marketing site. Three downstream WPs (WP-007a, WP-007b, WP-008) unblock immediately; the brand-tokens cross-site contract is now load-bearing rather than theoretical. The discovery that CF Pages `_redirects` doesn't support full-URL source patterns is the kind of design-vs-reality drift that an inline review pass can't catch — only execution surfaces it. The amendment preserves the in-repo reproducibility intent in spirit (Wrangler/Terraform export available) without forcing an emergency-override paper trail under the WP's "Temporary CF dashboard override" mechanism (which was designed for time-bounded emergencies, not for "the WP's premise was wrong"). The two zone-level settings (Browser Cache TTL, AI Crawl Control) are recorded explicitly because they are CF dashboard state that lives outside the repo and would silently regress contracts if a future operator reset zone defaults — the Decisions log is the only place this state is auditable from the repo. |
| 2026-05-09 | WP-007a paused at Step 1 pre-flight on first execution attempt — not locked. The locked CF Pages build command `pnpm install --frozen-lockfile && pnpm --filter @legendary-arena/arena-client build` (per WP-007a Step 7) fails to resolve `@legendary-arena/game-engine` on a fresh checkout, with Vite reporting `[commonjs--resolver] Failed to resolve entry for package "@legendary-arena/game-engine"`. **Root cause:** the engine package's `package.json` declares `"main": "./dist/index.js"`, but `packages/game-engine/dist/` is a build artifact directory and is gitignored — present in long-lived dev checkouts only because someone has run `pnpm -r build` at some point. The pnpm single-package filter (`--filter @legendary-arena/arena-client`) does NOT transitively build workspace dependencies, so on a fresh CF Pages clone (or any clean clone, including this session's worktree) the dist directory does not exist and the runtime import in `apps/arena-client/src/client/bgioClient.ts` (`import { LegendaryGame } from '@legendary-arena/game-engine'`) fails to resolve. `pnpm -r build` recursively builds all workspace packages and then arena-client builds successfully — that is how the existing main checkout has been operating, but it is not the locked WP-007a build path. **Secondary concern surfaced during the recursive-build verification (warnings, not errors):** `packages/game-engine/dist/scoring/scoringConfigLoader.js` imports `node:fs/promises`, `node:path`, `readdir`, `readFile`, and `join`, which Vite externalizes for browser compatibility with five warnings. The loader's author anticipated this and added a deliberate namespace-import workaround (`import * as fsPromises from 'node:fs/promises'`) so Rollup tree-shakes the unused functions out of the browser bundle — the warnings are expected and the bundle is correct, but the runtime / setup-tooling boundary is enforced implicitly by tree-shaking rather than explicitly by the package's `exports` field. **Decision:** WP-007a regresses to ⏸️ Pending pending **WP-144** (`docs/ai/work-packets/WP-144-arena-client-production-bundle-isolation.md` in the engine repo, drafted 2026-05-09, not yet executed) which (a) splits `packages/game-engine`'s `package.json` `exports` into a runtime entry (browser-bundle-safe) and a `./setup` subpath (Node-IO setup-tooling), (b) relocates `loadScoringConfigForScenario` / `loadAllScoringConfigs` behind the new subpath, (c) updates `apps/server/` import sites accordingly, (d) drops the namespace-import workaround in `scoringConfigLoader.ts` since the loader no longer reaches the browser-bundle import graph, (e) records a new architectural decision **D-14401** codifying the runtime / setup-tooling Layer Boundary contract, and (f) amends WP-007a's locked CF Pages build command to `pnpm install --frozen-lockfile && pnpm --filter "@legendary-arena/arena-client..." build` (trailing `...` is pnpm's topological selector — builds the package and its workspace dependencies in dependency order). After WP-144 lands, WP-007a re-executes with the amended build command and the rest of WP-007a's scope (Cloudflare Pages project creation, brand-tokens wiring, header/footer integration, custom domain binding, network-block test, Lighthouse) proceeds unchanged. **State at pause:** marketing repo clean (untracked `docs/ai/work-packets/WP-007a-play-deploy.md` is the WP body itself, deferred for commit until lock); engine repo clean across both main and the WP-007a session worktree (`claude/elated-booth-4e1c0f`, no source edits, parked for re-attempt). No CF Pages project created, no DNS changed, no commits landed. **Cross-site contract status:** WP-002 v1 LOCKED for WWW carve-out remains fully OPEN — the v1 cross-site contract was scheduled to half-close at WP-007a lock + fully close at WP-007b lock; with WP-007a paused, both halves remain open and `play.legendary-arena.com` is still `state: "planned"` in the engine repo's `docs/ops/domains.json`. **Pre-flight artifacts that DID validate cleanly** (preserved for the re-attempt): WP-006 CORS contract verified live from a synthetic `https://play.legendary-arena.com` origin (`Access-Control-Allow-Origin: *`, `Cache-Control: public, must-revalidate, max-age=3600`, `Version: v1` in body); foreign-origin `curl -I` and Origin-header probes both passed; PaperMod submodule still clean at `c4ca7ca486ecd67c8f6bba31551a6ee0d1455926`; WP-006 lock commit `b3d8c5d` confirmed on www `origin/main`. | This is the second design-vs-execution-reality discovery on the marketing-side deploy track in two days (after WP-006's CF-Pages-`_redirects`-doesn't-support-full-URL-sources finding). The pattern is the same: a WP that reads correctly on paper hits a constraint that only execution surfaces. The cost of pausing here is one extra WP and a few days; the cost of working around the locked build command (e.g., adding a non-locked CF prebuild step in the dashboard, or pre-building dist locally and pushing a "lock-pass" deploy that doesn't reproduce on CF) would be exactly the silent-dashboard-drift / same-commit-different-output failure mode that WP-007a Step 7's "Configuration immutability rule" exists to prevent. WP-144's wider-scope shape (runtime / setup-tooling subpath split, not just a topology-filter amendment) was chosen because the implicit tree-shaking boundary in the engine package was already a latent fragility — a future barrel re-export accidentally landing a Node-IO import inside the runtime path would silently inflate the SPA bundle and add more `__vite-browser-external` warnings, and there is no gate that would catch it. Codifying the boundary via `package.json` `exports` makes it testable. The v1 cross-site carve-out staying open through the pause is correct — half-closing it on a lock that didn't actually happen would lie about the contract's state. |
| 2026-05-09 | WP-144 receipt landed — post-execution amendment to WP-007a's locked CF Pages build command. The 2026-05-09 WP-007a pause entry (above) anticipated WP-144's topology-filter amendment as a marketing-side coordination receipt; that receipt is now applied here. Both `docs/ai/work-packets/WP-007a-play-deploy.md` and `docs/ai/invocations/session-wp007a-play-deploy.md` have every `pnpm --filter @legendary-arena/arena-client build` quote site replaced with `pnpm --filter "@legendary-arena/arena-client..." build` (15 + 11 = 26 occurrences total). A new "Post-execution amendment (2026-05-09; WP-144 backfill)" section near the top of WP-007a's body records the change and points back to engine-repo commits `bb0493c` (EC-144 implementation) + `8a0621a` (close) and to D-14401 in the engine repo's `docs/ai/DECISIONS.md`. Dev-server invocations (`pnpm --filter @legendary-arena/arena-client dev`) are intentionally NOT amended — `dev`'s fresh-tree fragility is a separate concern out of WP-007a / WP-144 scope. After this receipt lands, WP-007a's Step 1 pre-flight gates can be re-run with the amended build command and the rest of WP-007a's scope (CF Pages project creation, brand-tokens wiring, header / footer integration, custom domain binding, network-block test, Lighthouse) proceeds unchanged. WP-007a remains ⏸️ Pending in `03-ROADMAP.md` until execution actually completes; this receipt only unblocks the build command — it does not complete the deploy. | The receipt closes the loop on the 2026-05-09 pause entry by applying the amendment that entry described. WP-006's `_redirects` precedent already established that post-execution amendments to locked WP bodies are valid governance under this repo's "light governance" regime when the amendment records reality discovered during execution rather than redefines design intent. Same pattern here: the locked build command was correct in shape (single command, no extra steps, verbatim from local) but wrong in topology selector — fixed verbatim, not redesigned. The dev-server carve-out is recorded explicitly so a future executor doesn't read the amendment as an "always use topology filter for everything arena-client" rule and inadvertently change `pnpm dev` behavior. |
| 2026-05-12 | E-commerce scope expansion — "E-commerce, merch, paid content" removed from "Out of scope (v1)" list. Commerce capability added via WP-019 (Snipcart shopping cart with Stripe checkout). **Not a permanent non-goal reclassification** — the item was v1-scoped exclusion, not a permanent non-goal; permanent non-goals (line 131–140) are unchanged. **Static-only invariant preserved:** Snipcart is a client-side JS widget fetching from Snipcart's CDN infrastructure (`cdn.snipcart.com`), not from LA-owned APIs or services. The global invariant "No runtime APIs, no client-side data fetching from LA infrastructure" (line 41) is not violated — the constraint targets LA infrastructure specifically, and Snipcart is a third-party SaaS equivalent to the Google Fonts or Pagefind CDN assets already loaded. **Product data remains static:** catalog content lives in Hugo content files (`content/shop/*.md`) with front matter driving Snipcart's `data-item-*` HTML attributes at build time. No runtime data fetching, no database, no CMS. The site remains fully functional (product pages render, content is browsable) even if Snipcart's CDN is unavailable — only the cart widget and checkout flow are affected by a Snipcart outage. **"No margin, no mission" alignment:** this is the fastest path to revenue capture without a platform migration. Snipcart's bolt-on model preserves the Hugo + Cloudflare Pages architecture, the deterministic build contract, and the brand token API contract. The commerce layer is additive — removing it is a two-file revert (`extend_head.html` + `extend_footer.html`), not a platform unwinding. |
| 2026-05-12 | WP-016 locked — newsletter & blog templates. **`layouts/single.html` override:** PaperMod's `single.html` copied verbatim to `layouts/single.html` with a single insertion (`{{- partial "cta-block.html" . }}` after `.post-content`). Hugo silently discards content outside `{{- define "main" }}` blocks in override templates — the WP-016 spec's `<!-- WP-016 override ... -->` header comment had to be placed inside the block, not before it. This is a Hugo template-engine behavior constraint, not a design choice. **vitest `3.2.1` added to devDependencies** (exact pin, no caret) — 12 subscribe function tests, devDependencies-only, does not affect `npm run build` output. Build reproducibility verified post-addition. | `single.html` is the only full-template override in WP-016. Future PaperMod submodule updates require manual diff reconciliation against this file. The comment-placement discovery is recorded so a future executor doesn't re-encounter the "empty output" failure mode. |
| 2026-05-08 | Added WP-009 (class-color usage audit, cross-site) to roadmap as **DRAFT** — full spec in `docs/ai/work-packets/WP-009-class-color-usage-audit.md` pending review. Audit verifies the ten `--la-color-class-*` tokens (`palette.md §4.4`) are used correctly across all three consumer sites (`www`, `play`, `cards`) — Check 1: token definition integrity vs `palette.md §4.4`; Check 2: no raw class hexes outside `brand-tokens.css`; Check 3: every `--la-color-class-*` reference matches one of the four §4.4 application patterns (border-accent / chip-fill / icon-accent / selection-state) or is class-scoped; Check 4: no class tokens in brand identity, CTA backgrounds, semantic-state indicators, or link / focus-ring affordances. Depends on WP-007a + WP-007b. Roadmap Summary table updated; full section body deferred until draft is reviewed and integrated. Engine-side EC deferred until WP-007a/b actually land (drafting now risks staling against the not-yet-built UI surface). | The class-color subsystem (`palette.md §4.4`, locked 2026-05-08) ships ten new tokens with explicit role discipline and four named application patterns. Without a usage audit, downstream consumer integrations during WP-007a/b can silently introduce `palette.md §10` violations (class colors used as brand identity, CTA backgrounds, or semantic state indicators) that are expensive to find later. Drafting WP-009 now — before WP-007a/b execute — keeps enforcement intent visible to whoever runs those WPs and gives them a forward target. Read-only on consumer repos: WP-009 produces evidence and files tickets; remediation is a separate per-repo effort. First usage-discipline audit on the brand-tokens contract; if it lands clean, the same template applies to gradient / focus-ring / type-scale / spacing audits. |
| 2026-05-10 | WP-010 locked (header + footer site navigation). Site chrome now wires the existing `/about/` and `/posts/` pages via Hugo menus. Header: `[[menu.main]]` block in `hugo.toml` (About 10, Blog 20) consumed by the existing header partial — which already iterated `site.Menus.main` at `header.html` lines 104-128, byte-identical post-WP per DoD `git diff` check. Footer: `[[menu.footer]]` block in `hugo.toml` (About 10, Blog 20, Play 30, Cards 40) consumed by a new `layouts/_partials/footer.html` PaperMod override that adds `<nav class="footer-nav" aria-label="Footer">` as the first child of `<footer class="footer">`, preserving all upstream content (copyright + Hugo + PaperMod credits + scroll-to-top + theme-toggle + menu-scroll restore + code-copy scripts). External footer items (matched via `findRE "://" .URL`) carry `target="_blank" rel="noopener noreferrer"` plus the same external-link `<svg>` the header partial uses (`header.html` lines 117-123, byte-identical) for header/footer icon parity. Active-page indicator is a 2px `var(--la-color-blue-bright)` underline on the matching `<span class="active">` (per `palette.md §4.3` role discipline — interactive affordances use the bright variant; deeper `--la-color-blue` reads decorative post-2026-05-08 navy shift). **Home is intentionally NOT in the header menu** — the PaperMod logo (top-left, in `header.html` lines 17-49) already anchors to `/`, so adding a Home menu item would duplicate the affordance and dilute the menu's signal. **Footer menu uses `[[menu.footer]]` + an override** (not a `[params.footerLinks]` array) because PaperMod doesn't iterate `site.Menus.footer` natively, so an override was required either way; using the standard Hugo menus mechanism re-uses the conceptual model already in place for `site.Menus.main` and avoids inventing a new params surface for the same need. **Step 4.6 inline regression discovery + fix:** the first Lighthouse pass showed home Perf=77, CLS=0.344 — hard regression below the ≥ 90 floor AND below the pre-baseline 92. Diagnosis: (1) the `layout-shifts` audit attributed the entire 0.344 to `<main class="main">` — the whole `<main>` was moving down after first paint; (2) controlled before/after on the same commit (revert to HEAD, rebuild, re-Lighthouse) confirmed pre-WP home Perf=94-98, CLS=0.000-0.086 — regression was real, not environmental; (3) bisection isolated the cause to the `[[menu.main]]` additions in `hugo.toml` alone (removing `custom.css §8` left CLS at 0.344; removing the footer override left CLS at 0.344); (4) root cause: `layouts/_partials/extend_head.html` (outside this WP's allowlist) loads Google Fonts (Bebas Neue / Inter / JetBrains Mono) via `<link>` with `display=swap`; the WP-010 menu items render in Inter; on narrow viewports the Inter swap pushes the (logo + menu) combined width across `.header-nav`'s `flex-wrap` boundary, dropping the menu to its own flex-line ONLY after the font loads — that adds one row of header height post-paint, which shifts `<main>` down below it; (5) surgical fix within the WP-010 allowlist: `@media (max-width: 768px) { .header-nav > .menu { flex-basis: 100% } }` in `custom.css §8` forces the menu to its own `.header-nav` flex-line on mobile from first paint, so the row count is deterministic regardless of font state. Inter swap can resize menu text without rippling layout into `<main>`. Post-fix home Perf=94-99, CLS=0.000-0.046 across 3 runs. The fix carries a `// why:` comment block in `custom.css` so a future contributor encountering the rule can reverse-engineer the diagnostic chain without re-running the bisection. Mobile header is now 3 rows on `≤768px` (logo, menu, search) where pre-WP was 2 rows (logo, search) — a static layout change visible from first paint, not a CLS contributor. Desktop `≥769px` header remains single-row. **Lighthouse on production build at `http://127.0.0.1:1314/`** (mobile preset, 3-run sets; raw JSON gitignored per WP-005 convention): home 94/100/100/100, `/posts/` 95/100/100/100, `/about/` 92/100/100/100, `/posts/2026-05-07-launch-announcement/` 97/100/100/100. Pre-baseline (from pre-flight): home 92/100/100/100, posts 94/100/100/100, about 92/100/100/100. All four pages clear the 90 absolute floor AND the per-page pre-baseline. Reproducibility verified mechanically: two consecutive `npm run build` produce byte-identical `public/` (53 files, `Compare-Object` empty). Submodule clean (`c4ca7ca486ecd67c8f6bba31551a6ee0d1455926 themes/PaperMod (heads/master)`, no `+`). Header partial byte-identical (DoD check passed). **Commits:** `793e4bf` (SPEC bundle — WP body + roadmap row + WP-009 dep), `d571887` (WP-010 implement), and this lock commit. **Pre-flight artifact** at `docs/ai/invocations/preflight-wp010-site-navigation.md` is a scratchpad (gitignored per repo convention) — verdict was **READY TO EXECUTE** 2026-05-10 with two recorded caveats: (1) uncommitted SPEC bundle at audit time — resolved by the `793e4bf` commit at session start; (2) stale Hugo binary on user bash PATH (`C:\Users\jjensen\bin\hugo.exe` at v0.135.0 shadowing the WinGet v0.161.1 install) — worked around by using PowerShell which resolves to the WinGet binary unambiguously. The stale-bash-Hugo housekeeping is recommended cleanup but is not WP-010-specific. | Final piece of v1 site chrome. Pre-WP, `/about/` and `/posts/` shipped at 200 but were unreachable from the home page (`<ul id="menu">` rendered empty across every page); the visitor-reported gap on 2026-05-10 ("I don't see [the About page] on www.legendary-arena.com") was the immediate trigger. The font-swap CLS discovery during Step 4.6 is this WP's contribution to the "design-vs-execution-reality" pattern that WP-006 and WP-007a both encountered: a WP that reads correctly on paper hits a constraint that only execution surfaces. The fix preserves WP-010 scope (no `extend_head.html` edit, no `brand-tokens.css` edit, no font-loading-strategy change) and lives in `custom.css §8` with a self-documenting `// why:` comment block so a future contributor can reverse-engineer the rationale from the comment without re-running bisection. A site-wide `font-display` strategy revisit (`optional` vs `swap` vs self-hosted fonts with explicit metric matching) is recorded as a follow-up direction in the WP-010 roadmap detail Notes but deliberately deferred — it would be its own WP with broader scope than nav. **WP-009 unblocks downstream** per the Execution flow lock: with the v1 chrome stable, the class-color usage audit can now run against fully-built header + footer surfaces rather than a half-built chrome. The "Home is not in the header menu" decision is the kind of thing a future contributor might otherwise re-litigate (the absence of Home looks like an oversight to anyone who hasn't read the WP body); recording it here makes the rationale durable. Same for the `[[menu.footer]]`-via-override choice — `params.footerLinks` is the alternative most people would reach for first; recording why we didn't go that way prevents that conversation from recurring. |
| 2026-05-10 | WP-011 locked (`font-display: optional`). `layouts/_partials/extend_head.html` Google Fonts URL `display` query parameter flipped from `swap` to `optional` in a single-region, single-parameter value change — families (Bebas Neue / Inter weights 400/500/600/700 / JetBrains Mono weight 400), weights, separators, and encoding all preserved exactly; preconnect tags untouched. The §2 comment block in `extend_head.html` was amended in place to record the WP-011 rationale while preserving all existing context (Google Fonts use, family + weight inventory, the prior FOUT-over-FOIT note pointing to `typography.md §13`); added the `display=optional` semantics, the WP-010 § Step 4.6 diagnostic citation, and the slow-network trade-off accepted explicitly. **Lighthouse on production build at `http://127.0.0.1:1314/`** (mobile preset, same headless `lighthouse@12` invocation as WP-005 / WP-006 / WP-010): home 96/100/100/100 CLS 0.000, `/posts/` 100/100/100/100 CLS 0.000, `/about/` 100/100/100/100 CLS 0.000, `/posts/2026-05-07-launch-announcement/` 99/100/100/100 CLS 0.000. Deltas vs the WP-010 lock pre-baseline: home P +2 CLS −0.046, posts P +5 CLS −0.005, about P +8 CLS flat at 0, post P +2 CLS −0.004. Every category clears both the 90 floor AND the per-page WP-010 lock baseline; CLS hard gate (≤ lock value per page) is satisfied AND the expected-trend prediction toward 0 is achieved on every page. Lighthouse `audits.font-display` returns `score=1, items=0` on every page — the browser-side confirmation that `optional` is the value actually in force. **Decision rationale:** chose `optional` over `swap` (which preserved the swap window that caused the WP-010 Step 4.6 regression), over `block` (which yields FOIT — Flash of Invisible Text — bad UX on slow connections), and over `fallback` (which still permits a small swap window within ~3 s post-load). `optional` is the only value that guarantees zero swap events post-commit-point: the browser gives the font ~100 ms to load, commits to either the web font or the system-ui fallback, and never swaps after that. No swap event = no font-swap CLS as a class of regression. **Trade-off, explicitly accepted:** slow-connection users see the system-ui fallback for the entire session (not just initial paint). Brand-permitted — `strategy.md §10` does not list "system-ui rendering" as a brand failure mode, and the `--la-font-*` fallback chain in `static/brand-tokens.css` is explicit (per `typography.md §3.1 / §3.2 / §3.3`: Bebas Neue → Anton → Oswald → system-ui → sans-serif; Inter → system-ui → -apple-system → Segoe UI → sans-serif; JetBrains Mono → IBM Plex Mono → Consolas → monospace). Future direction: self-hosting fonts under `static/fonts/` would add CSS Fonts Level 4 metric-matching (`size-adjust` / `ascent-override` / `descent-override` / `line-gap-override`) that eliminates even the optical shift between fallback and web-font rendering — a WP-012-shaped follow-up when the bundle-weight + CORS-posture cost is warranted, not a WP-011 concern. **Not a WP-002 v1 → v2 brand-tokens bump.** The font-family tokens (`--la-font-display`, `--la-font-body`, `--la-font-mono`) are unchanged byte-for-byte; only the loading behavior of the same font files changed. `git diff static/brand-tokens.css` was empty at lock time. No `Version:` bump in `brand-tokens.css`; no coordinated consumer rollout required for `play.legendary-arena.com` or `cards.barefootbetters.com`. Each consumer can independently adopt `display=optional` in its own WP at its own discretion. **The WP-010 `custom.css §8` mobile-wrap fix stays in place.** Even with the font-swap enabler gone, the surgical fix (`@media (max-width: 768px) { .header-nav > .menu { flex-basis: 100%; } }`) is harmless under `display=optional` — deterministic flex-wrap behavior is a UX improvement once the root cause is gone. Removing it would have been a separate scope-creep edit and is deferred to a future audit if it's ever determined redundant. `git diff assets/css/extended/custom.css` was empty at lock time. **Slow 3G interactive test was replaced with mechanical equivalents.** The WP § Step 2.2 named a DevTools Network → Slow 3G throttle to confirm the no-swap behavior; this lock pass relied on (a) URL-shape regex match on the served HTTP body (`&display=optional` present exactly once, no `&amp;display` encoding drift, no duplicate `display` params, `display=swap` count = 0 across all 9 rendered HTML files), and (b) Lighthouse's `font-display` audit returning `score=1, items=0` on every page (browser-side observation of the `optional` value actually being in force). The W3C/Chromium spec for `display=optional` guarantees the no-swap posture by specification once the URL is correct; no implementation drift is possible without a browser bug. A future executor can run the manual Step 2.2 protocol on the production deploy after Cloudflare Pages auto-redeploys — behavior should match the mechanical proof, and a divergence would be a browser bug, not a WP regression. **Allowlist compliance:** `git diff --name-only main...HEAD` at lock time showed exactly three paths — `layouts/_partials/extend_head.html`, `docs/03-ROADMAP.md`, `docs/01-VISION.md` — matching the pre-flight Scope Lock allowlist exactly. **Mechanical reproducibility:** two consecutive `npm run build` produced byte-identical `public/` (53 files, `Compare-Object` empty) — matches the WP-005 / WP-006 / WP-010 lock-pass methodology. **Submodule:** `c4ca7ca486ecd67c8f6bba31551a6ee0d1455926 themes/PaperMod (heads/master)` with no `+` flag throughout. **Commits:** `0929b48` (initial SPEC), `bcc41a6` (hardening pass per audit review), and this lock commit (implement + lock). **Pre-flight + session prompt** at `docs/ai/invocations/preflight-wp011-font-display-optional.md` + `session-wp011-font-display-optional.md` are scratchpads (gitignored per repo convention); verdict was **READY TO EXECUTE** 2026-05-10 with one informational caveat (the SPEC hardening pass was uncommitted at audit time — resolved by the `bcc41a6` commit before this session). | First prophylactic infrastructure WP on the marketing site. WP-006 → WP-010 each surfaced a "design-vs-execution-reality" pattern (CF Pages `_redirects` mechanism, font-swap CLS); WP-011 closes the font-swap class of bug at the source rather than mitigating symptoms surface-by-surface. The WP-010 surgical fix in `custom.css §8` was correct for WP-010's verification pass but did not prevent the same class of bug from recurring on any future WP that adds text-rendering UI elements in regions that affect `<main>` positioning — WP-008 (SEO baseline) or any future blog-template / breadcrumb / homepage-card work would have inherited the same hazard. WP-011 removes that hazard from every future WP's verification budget. The "expected-trend prediction validated" outcome (CLS = 0.000 on every page, not just at-or-below the lock value) is the strongest possible evidence that the WP's premise was correct: font-swap was the dominant remaining CLS source on www, and eliminating it took every page to zero. The performance lift on every page (4 of 4 pages improved, /about/ jumping the full 8 points to a perfect 100) is the bonus side effect of removing one render-blocking event per page. The trade-off acceptance is the most important load-bearing fact for future readers: a future reviewer who notices that slow-connection users see the system-ui fallback for the entire session might flag this as a regression and ask to revert — this Decisions log entry plus the comment block in `extend_head.html` together make it explicit that the trade-off was named-and-accepted at lock time, grounded in `strategy.md §10` and the explicit fallback chain in `brand-tokens.css`. The mechanical-vs-interactive Slow 3G note is also load-bearing for honesty: future executors should know which DoD items were verified by spec-compliance argument vs direct observation, so the standard for "verified" doesn't drift between WPs. |
| 2026-05-12 | WP-009 locked (class-color usage audit, cross-site). All five checks PASS with zero violations across all three consumer repos (`www` at `56ac8ec`, engine monorepo at `6f5c72a` covering `arena-client` and `registry-viewer`). **Check 1 — Token definition integrity:** all ten `--la-color-class-*` tokens present in `static/brand-tokens.css` with hex values byte-identical to `palette.md §4.4`. Mode-stability verified (standalone `:root` block, not overridden in `html[data-theme="dark"]`). **Check 2 — No raw class hexes:** zero Bucket 1 (CSS property) or Bucket 2 (programmatic styling) violations. All raw hex occurrences outside `brand-tokens.css` / `palette.md` are content-display (Bucket 3): `/brand/` page's `palette.svg` swatch image, Decisions log prose in `01-VISION.md`, docs-only SVG reference. Consumer fallback files (`brand-tokens.local.css` on `arena-client` and `registry-viewer`) are token definition snapshots per the WP-007a/WP-007b SHA-256 hash-parity contract — classified as excluded (equivalent to `brand-tokens.css`). **Check 3 — Pattern compliance:** zero `--la-color-class-*` token references in www layouts or CSS property values. Registry-viewer uses five bright class-color tokens via `HC_COLOR` constant in `src/lib/theme.ts` (lines 39-43), consumed at three Vue template sites: `CardDetail.vue:160`, `CardDetail.vue:234`, `CardGrid.vue:123` — all apply class color as text `color` on elements semantically identifying a specific hero class; all match the **icon-accent** application pattern from §4.4. Arena-client has zero class-color token references in source code at v1. **Check 4 — Disallowed contexts:** zero §10 violations. No class-color token used as brand identity, CTA background, semantic state indicator, or interactive affordance (link/focus ring) on any consumer. **Check 5 — `/brand/` content vs styling boundary:** PASS. `layouts/brand/list.html`, `layouts/_shortcodes/brand-swatch.html`, `content/brand/_index.md`, and `assets/css/extended/custom.css` confirmed: class colors appear only as swatch display content and documentation text, never as brand-page surface styling. Findings doc at `docs/brand/audits/class-color-usage-v1.md` with full `file:line` evidence and reproducible RG recipes. This establishes the v1 class-color usage baseline. No remediation tickets required. | First enforcement audit on the brand-tokens contract. The clean result validates that the class-color subsystem (`palette.md §4.4`, added 2026-05-08) was integrated correctly during WP-007a (play) and WP-007b (cards), and that WP-014's `/brand/` page maintains the content-vs-styling boundary. The audit template (five checks, three-bucket classification, §10 prohibitions) is reusable for future token-discipline audits (gradient, focus-ring, type-scale, spacing). The findings doc is the first point-in-time baseline; a future CI enforcement check (regex-based) is a candidate v2 effort now that the surface area is confirmed small enough to be tractable. |
| 2026-05-12 | WP-014 locked (public `/brand/` page + mood-board brief). Five canonical decisions land with this WP. **(1) `/brand/` is a separate surface from `/about/`** because the audiences split: `/about/` is narrative-only for general visitors ("who is Legendary Arena, why does it exist"), while `/brand/` is a structured spec surface for external creators / partners (mod authors, content creators, press, integration partners) who need durable, deep-linkable references to the brand system. The two surfaces co-exist in the header (About / Blog / Brand at weights 10 / 20 / 30) and the footer (About / Blog / Brand / Play / Cards at 10 / 20 / 25 / 30 / 40) rather than `/brand/` folding into `/about/`. Pre-WP review (2026-05-11) considered the alternative and explicitly rejected it: forcing brand-system structured spec content into a narrative About page would either bloat About past its narrative remit OR force the brand-system content into a deeper `/about/brand/` sub-path that loses the deep-link affordance external creators need. The split is the simpler model and stays out of `/about/`'s way. **(2) The §Token Contract section of `/brand/` is the public statement of the cross-origin token-consumption invariant.** Pre-WP-014, the invariant lived in three private docs: `01-VISION.md §Cross-site contract`, `static/brand-tokens.css` header comment, and `docs/brand/CHANGELOG.md` v1 lock entry. WP-002 locked the surface, WP-006 wired the cross-origin Cloudflare deploy (`Access-Control-Allow-Origin: *` on `/brand-tokens.css` + `Cache-Control: public, max-age=3600, must-revalidate`), and WP-007a/b verified end-to-end consumer integration at `play.legendary-arena.com` and `cards.barefootbetters.com` — but the contract was never restated as a thing external creators could read. `/brand/ §Token Contract` is now that restatement: "All visual styling MUST consume the canonical tokens defined in `/brand-tokens.css`. No raw hex values. No ad-hoc typography. All consumers (`www.*`, `play.*`, `cards.*`) consume tokens via cross-origin link." This is presentation only — it changes nothing about the contract — but it makes the contract discoverable to anyone reading `/brand/` without having to read `01-VISION.md` or trace through three WP bodies. **(3) Class colors stay gameplay-only and the brand page reinforces that boundary.** `palette.md §4.4` ships ten `--la-color-class-*` tokens (strength / covert / instinct / ranged / tech, each with bright + muted variants) that identify a hero's gameplay class on chips / filters / deck-builder / class borders. `palette.md §10` lists "class color used as brand identity" as a hard failure mode. Pre-WP-014, that boundary lived in `palette.md` only. `/brand/ §Color System` now renders class colors as swatch *content* inside the "Full spec" collapsible with the explicit "gameplay-only" annotation prominently displayed; the brand-page surface uses generic `--la-*` tokens (`--la-color-gold`, `--la-color-red`, `--la-color-blue`, `--la-color-cta`, etc.) and never `--la-color-class-*`. Section 7 §Usage Guidelines DON'T card 1 ("DON'T use class colors for branding") states the rule explicitly for external creators. This restatement defends against a future contributor (with or without the brand docs open) promoting class colors to brand-identity colors in `mood-board-spec.md` future revisions or in any consumer surface; the brand page now serves as the public anchor of the boundary. **(4) `docs/brand/mood-board-spec.md` is now a canonical brand doc, sibling to `strategy.md` / `palette.md` / `typography.md` / `spacing.md`.** Pre-WP-014, the canonical brand-doc cluster was the four locked Phase B docs above + `CHANGELOG.md`. The mood-board brief was floating as Appendix A of the WP body and would have remained inline-only (re-authored every time a poster commission happened). WP-014 commits the brief as `docs/brand/mood-board-spec.md` — same parent directory, same role, same authority chain (subordinate to `01-VISION.md` + `docs/brand/strategy.md`, with the locked Phase B docs as references) — so it is durable across artist commissions and re-engagements. The doc is dual-purpose: standalone reading (linked from `/brand/`'s §Art Direction preamble), and inline rendering inside `/brand/ §Art Direction` via the `readfile` shortcode (Path A — see decision 5). The doc's seven content blocks are H3-level so they nest cleanly under the page's H2 `#art-direction` when inlined; the doc's H1 was dropped during execution to keep heading hierarchy valid (the original Lighthouse Accessibility audit flagged `heading-order` on the H2 → H1 jump; dropping the H1 + moving metadata to an italic footer block fixed the violation while preserving doc-as-standalone readability). No `docs/brand/CHANGELOG.md` entry was added — the brief does not change any token value and the CHANGELOG is scoped to token-surface changes only. **(5) Three execution-time mechanism choices are recorded for re-runnability.** **Path A retained for Section 8 rendering.** WP §Step 3 §8 specified Path A (a new `layouts/_shortcodes/readfile.html` shortcode that inlines `docs/brand/mood-board-spec.md` at build time via Hugo's `readFile` + `markdownify`) as preferred, with Path B (manual mirror in `content/brand/_index.md` with a sync-reminder comment in both files) as the fallback if reproducibility broke. Path A held: two consecutive `npm run build` produced byte-identical `public/` across all 63 emitted files (`Compare-Object` empty); the mood-board-spec.md → /brand/§8 propagation was verified by edit-rebuild test. Path A wins on automatic sync (one edit, two render targets) and `Compare-Object`-clean determinism; the file path is project-root-relative (`/docs/brand/mood-board-spec.md`), no env-specific paths, no timestamps. **Mechanism B for `.brand-page` root-class application.** WP §Step 5 named two acceptable mechanisms: A) frontmatter + theme body-class hook driven by `bodyClass: brand-page` frontmatter (preferred if PaperMod's default `single.html` supports it), or B) a dedicated layout override that wraps the page content in `<div class="brand-page">`. Mechanism A is not viable because PaperMod's default templates (`themes/PaperMod/layouts/baseof.html`, `single.html`, `list.html`) expose no frontmatter-driven body-class or wrapper-class hook, and modifying `layouts/baseof.html` to add one would be outside the WP-014 allowlist (the override exists in the project at `layouts/baseof.html` but does not host such a hook). Mechanism B landed at `layouts/brand/list.html` — Hugo's section-scoped layout lookup picks `layouts/<section>/list.html` ahead of `_default/list.html` and the theme's copy, so the override applies only to `/brand/`. The WP body authorises "`layouts/brand/single.html` or equivalent"; for an `_index.md`-driven section home, the equivalent is `list.html` (Hugo's `single.html` lookup never fires for a section home page). The override wraps PaperMod's standard section content (`breadcrumbs.html` + `<h1>` + description + `.post-content` via `anchored_headings.html`) in `<div class="brand-page">`; PaperMod's child-page iteration loop is intentionally skipped because `/brand/` has no child pages. `layouts/_partials/header.html` byte-identical post-WP (DoD `git diff` empty). **Logo asset paths under `static/brand/logo/`.** Three SVGs copied byte-identical from `docs/brand/logo-figma/` (the working source — copy-only relationship, not a move): `static/brand/logo/logo-la-dark-400x200.svg` (primary, gold on dark — rendered on `/brand/ §Logo + Identity`), `static/brand/logo/logo-la-light-400x200.svg` (white-only inversion — referenced as the inversion form, only used when a light surface forces inversion), `static/brand/logo/legendary-arena-icon.svg` (icon / favicon — referenced for tight-square contexts). The fourth canonical source `logo-la-light-400x200-color.svg` is NOT used by this WP per the WP body's explicit carve-out. Future logo-bump WPs must update both `docs/brand/logo-figma/` (working source) and `static/brand/logo/` (published copies) — recorded here so the double-tracking is intentional and not a forgotten artifact. **Mid-execution governance amendment: `[markup.goldmark.renderer] unsafe = true` in `hugo.toml`.** The first production build emitted a single `WARN  Raw HTML omitted` warning, dropping the WP-mandated `<details>` collapsibles (`/brand/ §Color System` + §Typography "Full spec"), the DO/DON'T `<ul class="usage-rules">` (§Usage Guidelines) with inline SVG icons, and the `<div class="brand-swatch-grid">` wrappers around the brand-swatch shortcode emissions. The fix is an additive `[markup.goldmark.renderer]` sub-block in `hugo.toml`, sibling to the existing `[markup.highlight]` block — not a touch of `[params]` or any WP-004 / WP-005 / WP-006 / WP-010 commentary block (the session prompt's narrower restriction). The amendment is recorded here for the same governance reason as WP-006's `_redirects` mechanism change: when execution surfaces a constraint that prevents the WP from delivering its mandated content as authored, the smallest scope-compliant fix that lets the WP land is the correct response, with the amendment recorded in the Decisions log so a future contributor can trace why the marker exists. Other pages (`/`, `/about/`, `/posts/`, posts) author no raw HTML and are unaffected by the unsafe flag. **Lighthouse on production build at `http://127.0.0.1:1314/`** (mobile preset, single-run per page with stale-Chrome process cleanup between runs; raw JSON gitignored): `/brand/` 99 / 100 / 100 / 100, `/` 99 / 100 / 100 / 100, `/posts/` 100 / 100 / 100 / 100, `/about/` 99 / 100 / 100 / 100. All four pages clear the ≥ 90 absolute floor AND the per-page pre-baseline (home 94, posts 100, about 93 from the preflight at HEAD `a0b3525`). The `/brand/` page establishes its own ≥ 90 floor at 99 / 100 / 100 / 100. An earlier round of measurements showed `/brand/` Perf 88-89 — the root cause was Lighthouse's EPERM-on-tmp-cleanup error leaving stale Chrome processes accumulating across consecutive runs, competing for CPU and skewing the throttling math. After `Stop-Process chrome*` between runs, scores stabilised at 99 across three reproducible iterations; the fix is execution-environment hygiene, not a site change. **Mechanical reproducibility:** two consecutive `npm run build` produced byte-identical `public/` (63 files, `Compare-Object` empty). **Submodule:** `c4ca7ca486ecd67c8f6bba31551a6ee0d1455926 themes/PaperMod` with no `+`. **Commits:** `a3aec2f` (SPEC #11), `a0b3525` (SPEC hardening pass #12), `6f856f3` (WP-014 implement), and this lock commit. | The brand page is the public anchor of decisions that previously lived only in private docs (`01-VISION.md §Cross-site contract`, `palette.md §4.4`, `palette.md §10`, the brand-tokens header comment). Pre-WP, an external creator wanting to interpret the brand had to either guess from the rendered site or be handed `docs/brand/*.md` directly (which is a contributor-facing surface, not a creator-facing one). Post-WP, `/brand/` is the single public surface that answers "how do we use your visual identity, and how do we direct art for it" — `/about/` answers "who are you," and the two stay distinct. The mood-board brief promotion to canonical brand doc closes a gap that would otherwise have re-authored the brief inline every commission cycle. The Path A choice (and the Decisions-log record of it) means a future poster-artwork or mood-board revision is a single-file edit that auto-propagates to `/brand/ §Art Direction` without manual mirror maintenance; the WP-009-style "audit Section 8 for drift from `mood-board-spec.md`" check is unnecessary by construction. The Mechanism B record matters because someone re-reading the layout structure might wonder why `/brand/` has its own `layouts/brand/list.html` while `/about/` doesn't — recording the PaperMod-doesn't-expose-a-frontmatter-hook reason here means that conversation doesn't recur. The class-color gameplay-only restatement is the durable defense against the failure mode `palette.md §10` already lists; surfaces multiply over time, and the brand page is the one a new contributor is most likely to read when wondering "can I use these colors for marketing?" — the inline annotation answers that without requiring them to read `palette.md`. The goldmark-unsafe amendment is recorded for the same reason WP-006's `_redirects` amendment is recorded: future readers of `hugo.toml` will see the `unsafe = true` flag and may wonder if it's a security regression — the entry here makes the trade-off explicit (authored content is governed input; the alternative was either dropping the WP-mandated raw HTML or proliferating shortcodes outside the allowlist for every collapsible / list-with-icons construction). |

| 2026-05-13 | **WP-018 — Brevo email engagement pipeline (2026-05-13)** Brevo email engagement workflow configured. Welcome email automation active (1 trigger, 1 action, no branching — determinism invariant). Weekly cadence is manual broadcast (not automated drip) to preserve pre-send QA gates. Domain authentication (SPF/DKIM/DMARC) verified for legendary-arena.com. Sender identity: `newsletter@legendary-arena.com`. Funnel architecture documented at `docs/brevo/email-automation.md` covering pipeline stages (capture → confirm → activate → engage → convert → retain), CTA hierarchy (per WP-020), subscriber state model, weekly send procedure with pre-send QA + funnel integrity check gates, UTM conventions, and compliance posture. Welcome CTA invariant: always "Play now" → `play.legendary-arena.com`, no rotation. Template version pinned to `docs/brevo/newsletter-template.md` v2 (WP-020). | Configures the Brevo-side engagement pipeline that sits between WP-015's capture infrastructure and WP-017's content pipeline. The manual-broadcast decision for weekly sends is the most load-bearing choice: automated drip would bypass the pre-send QA checklist and funnel integrity check gates that WP-020 established. The welcome email is the subscriber's first branded touchpoint after the transactional double-opt-in confirmation — it must be deterministic (1 trigger, 1 action) and consistent (always "Play now" CTA, no rotation) to ensure reliable first-session activation. `docs/brevo/email-automation.md` is the operational reference for anyone managing the newsletter going forward; it anchors the Brevo configuration state with template ID, workflow ID, and list ID so future WPs can verify continuity. WP-021 (funnel analytics baseline) is now unblocked. |
| 2026-05-13 | **WP-020 — Newsletter & blog funnel enrichment (2026-05-13)** Moved "Share" from primary CTA (newsletter template §6) to secondary module (§9). Primary CTAs now match archetype contract: play/newsletter/tournament. Added "Featured from the Shop" (§8) and "Share / Forward" (§9) as secondary modules with hard caps (1 shop link, 1 share link per edition). Introduced UTM tracking contract for shop links. Amended WP-017 spec to include funnel hierarchy, blog "Gear up" shop section, newsletter secondary modules, and expanded funnel integrity check with QA log. | Governance-only WP aligning newsletter template with archetype contract and enriching the content pipeline spec with secondary conversion modules before WP-017's next content batch executes. The "Share" CTA option in §6 was never implemented in `cta-block.html` and conflicted with the archetype's `cta` field values; moving it to a secondary module resolves the inconsistency. Shop and share modules are secondary by design — hard caps (1 each) and placement constraints prevent them from competing with the primary CTA. UTM tracking contract enables attribution without runtime analytics. |
| 2026-05-14 | **WP-021 — Funnel analytics baseline (2026-05-14)** Defined measurement contract for the email funnel: six stages (Capture, Confirm, Activate, Engage, Convert, Retain) with explicit metric formulas using Brevo-native unique counts. Conversion rate marked NOT YET MEASURABLE pending analytics platform selection. Baseline failure thresholds established for all measurable metrics (validation ranges, not success targets). Created append-only metrics log (`docs/email-metrics-log.md`) with per-send template including raw counts, reproducible derived metrics, UTM validation, threshold checks, and PASS/FAIL result. Snapshot semantics: metrics recorded ≥48h post-send and immutable after recording. | Governance-only WP converting the WP-018 engagement pipeline into a measurable, auditable system. All metrics are Brevo-native — no external tools, no inferred metrics. The measurement conventions enforce unique counts throughout (not totals) to prevent silent metric inflation. Baseline thresholds are framed as failure-detection ranges, not success targets — the distinction matters because early-stage data will be noisy and treating ranges as targets would generate false optimization pressure. The append-only metrics log with raw counts + reproducible derived metrics ensures every recorded percentage can be independently verified. UTM attribution validation uses URL string inspection (not visual rendering or redirect assumptions) per WP-020's contract. Site-side conversion tracking is deferred to a future analytics platform WP; the UTM params are already flowing so attribution data will be available when a platform is chosen. |
| 2026-06-08 | **Diorama product line placed.** Canonical spec lives in `docs/product/` (`diorama-master-plan.md` authoritative; `diorama-gtm-and-resourcing.md` holds ICP / positioning / break-even). The website's role is to **market and sell** the line — diorama product/landing pages, shop via WP-019 Snipcart, a STEM/build content lane, and a diorama-ICP email funnel (extends WP-018/020) — **not** to build the physical kits, firmware, or curriculum. Website-facing slices enter `03-ROADMAP.md` as roadmap-designed WPs; the physical/firmware/curriculum build is tracked in the product plan's §13 and (for any Pi software) the engine repo. | A whole revenue line was absent from the site vision, leaving its boundary undefined. This fixes it: the marketing repo owns the *selling* surface, while the 73 KB hardware/STEM spec and the physical/firmware build stay out of website governance. Commerce is already in-scope (see 2026-05-12 e-commerce expansion / WP-019), so selling the line needs no further scope change — only the website WPs that ROADMAP will carry. |
| 2026-07-16 | **Registry domain migrated: `cards.barefootbetters.com` → `cards.legendary-arena.com`.** Supersedes the 2026-05-07 "Registry remains at `cards.barefootbetters.com` for v1" entry. The migration landed on `cards.legendary-arena.com`, not the originally projected `registry.legendary-arena.com`. Nav links repointed under WP-034; current-state docs swept under WP-035 (historical records untouched). `https://cards.legendary-arena.com/` verified live (HTTP 200) 2026-07-16. **Old domain still serves 200 with no redirect** — 301 posture is an open item owned on the CF/engine side, tracked in `docs/corporate-memory/03-open-questions.md`. **`static/brand-tokens.css` header comment deliberately NOT updated:** the served file is SHA-256 hash-parity-checked against bundled consumer fallbacks (WP-007a contract, hash `70C11CEB…13FF`); a comment-only edit would trip the parity trip-wire and force a coordinated engine-repo refresh for zero functional gain. The comment rides along with the next real token change. | Aligns the registry with the LA brand — the reason the migration was planned all along. Documenting the `registry.*` → `cards.*` naming change prevents future readers from treating `registry.legendary-arena.com` as still-planned. Recording the missing redirect keeps the duplicate-content/SEO exposure visible until closed. |
| 2026-07-16 | **Redirect closed (same day):** 301 Redirect Rule deployed on the `barefootbetters.com` CF zone — rule "Redirect cards.barefootbetters.com to cards.legendary-arena.com", filter `http.host eq "cards.barefootbetters.com"`, dynamic target `concat("https://cards.legendary-arena.com", http.request.uri.path)`, 301 permanent, preserve query string. Verified live: `/` and `/sets/?q=test` both return 301 with path + query preserved. Same mechanism as WP-006's apex→www rule and recorded here for the same reason: zone-level CF dashboard state lives outside the repo and would silently regress if a future operator reset zone rules. The Pages custom-domain binding for `cards.barefootbetters.com` remains in place (the Redirect Rule fires at the edge before Pages routing; the binding keeps DNS/cert posture intact). | Closes the duplicate-content/SEO exposure recorded in the entry above. The old domain is now a permanent alias, not a second canonical. |

## What's locked vs. TBD

**Locked:**
- Site purpose, audience, architecture
- Hosting (Cloudflare Pages, three projects)
- Theme approach (PaperMod upstream submodule + overrides)
- Repo location (`legendary-arena/legendary-arena-website`)
- Search (Pagefind, content only)
- Brand-token contract (externalized, versioned, cross-origin consumable)
- LA brand identity v1 — palette, typography, spacing (WP-002 + WP-003 lock 2026-05-07; tonal iteration 2026-05-08 under `palette.md §9.1`)
- Color-system architecture — three layers: **brand identity** (3 colors: gold + maroon + navy), **class-color subsystem** (10 tokens for 5 hero classes — strength, covert, instinct, ranged, tech), **semantic colors** (success / warning / error). Brand stays restrained; gameplay is expressive. Asymmetry intentional.
- Failure-isolation principle
- Global invariants
- SEO baseline (per WP-008)

**TBD (with owning effort):**
- Logo design — design contract written (`docs/brand/logo-brief.md`); two AI exploration rounds archived (`docs/brand/logo-explorations/`). Next step: Figma session or contractor pass starting from the "D done with B's discipline" merge thesis (`docs/brand/logo-ai-workflow.md §7`). Wordmark placeholder remains active on `www.legendary-arena.com` until a candidate passes `logo-brief.md §9` acceptance.
- Site copy and content beyond home/about/first blog post (WP-004)
- ~~Migration of registry from `cards.barefootbetters.com` to `registry.legendary-arena.com` (deferred future effort; not blocking v1)~~ **DONE 2026-07-16** — landed on `cards.legendary-arena.com`; old domain 301-redirects (zone Redirect Rule, same day; see Decisions log)
- Class-color UI integration on `play.*` and `registry.*` (post-WP-007 — usage patterns from `palette.md §4.4` provide the visual grammar; no engine changes for branding alone)
- Launch date

---

## Notes for future authors

- Update this document when a decision changes — don't let it drift.
- New decisions get a row in the Decisions log with date and rationale.
- If a Work Packet is being written that contradicts something here, the
  contradiction is a flag: either the WP needs to change, or this
  document needs to change. Resolve before executing the WP.
- Treat Global invariants as bright lines, not aspirations. If a feature
  cannot be built without violating one, the feature does not ship in
  its proposed form.
